# امنیت Portal AI

هیچ سیستم عمومی را نمی‌توان «صددرصد امن» نامید؛ این پروژه دفاع چندلایه، ثبت‌پذیری و توقف سریع خسارت را پیاده می‌کند.

## قبل از عرضه

- Secretهای تولیدشده، اتصال دیتابیس/Redis/S3 و کلید Liara AI فقط در Environment لیارا باشند؛
- `PAYMENTS_ENABLED=false` بماند تا درگاه و قیمت واقعی مدل‌ها تست شوند؛
- Backup روزانه PostgreSQL و هشدار هزینه/منابع در لیارا فعال باشد؛
- مدیریت فعلی IP-only است؛ `ADMIN_IP_ALLOWLIST` را فقط روی IP عمومی ثابت و اختصاصی بگذارید. مقدار خالی، مدیریت را برای همه می‌بندد؛
- افراد پشت IP خروجی مشترک هم دسترسی مدیر دارند؛ این مدل رمز یا MFA فردی ندارد. مسیر ingress و صحت هدر IP باید بررسی شود؛
- محدودیت روزانه کل سیستم ابتدا پایین باشد و بعد از مشاهده مصرف واقعی افزایش یابد.

## محافظ‌های فعال

- Base URL فقط از `AI_BASE_URL` سمت سرور خوانده می‌شود و دامنه‌اش با `AI_PROVIDER_HOST_ALLOWLIST` اعتبارسنجی می‌شود؛
- کلید ارائه‌دهنده با AES-256-GCM رمز می‌شود و به مرورگر برنمی‌گردد؛
- در حالت فعلی صفحه و API مدیریت IP مجاز را بررسی و برای دیگر IPها پاسخ 404 می‌دهند؛
- درخواست‌های تغییردهنده مدیریت علاوه بر IP، مبدأ درخواست را بررسی می‌کنند؛
- پیش از شروع production، origin امن، رازهای متفاوت، کلید رمزگذاری معتبر و تنظیمات سرویس‌ها بررسی می‌شوند؛
- CSRF، Secure/HttpOnly/SameSite cookies، rate limiting و Audit Log فعال‌اند؛
- پاسخ‌های خصوصی و assetهای کاربر cache نمی‌شوند و Service Worker فقط فایل‌های عمومی امن را نگه می‌دارد؛
- رزرو اعتبار و هزینه اتمیک است و درخواست شکست‌خورده بدون خروجی اعتبار را پس می‌دهد؛
- URL تصویر و مسیرهای ارائه‌دهنده در برابر SSRF اعتبارسنجی می‌شوند و redirectهای fetch خارجی رد می‌شوند؛
- قطع‌کننده سراسری هزینه مستقل از سهمیه کاربران است.

## اگر کلید یا حساب مشکوک شد

1. از پنل مدیریت «توقف اضطراری» را فعال کنید.
2. کلید Liara AI را در پنل ارائه‌دهنده لغو و کلید تازه بسازید.
3. IP مشکوک را از allowlist حذف کنید، مسیر خروجی مدیر را بررسی و نشست‌های کاربران مرتبط را ببندید.
4. رخدادهای امنیتی و Audit Log را بررسی کنید.
5. هزینه ثبت‌شده در Liara AI و دفترکل Portal AI را مقایسه کنید.
6. پس از رفع علت، سرویس را با سقف روزانه پایین دوباره فعال کنید.

قطع‌کننده داخلی جایگزین سقف و هشدار هزینه داخل حساب ارائه‌دهنده نیست؛ هر دو را فعال نگه دارید.

## گزارش آسیب‌پذیری

جزئیات آسیب‌پذیری، کلید یا داده کاربر را در issue عمومی منتشر نکنید. گزارش باید شامل مسیر، اثر، مراحل بازتولید بدون داده واقعی و پیشنهاد کاهش ریسک باشد.

## انتشار و محدودیت تأیید

راهنمای جاری استقرار، کنترل proxy، migration و آزمون‌های سرویس واقعی در `LIARA-DEPLOYMENT.md` است. فایل‌های گزارش قدیمی با شماره نسخه، گزارش همین انتشار نیستند. تست‌های مستقل جای آزمون نفوذ، بررسی واقعی ingress و تست دیتابیس/پردازش مالی در محیط مقصد را نمی‌گیرند.
