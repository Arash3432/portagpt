# خطای استقرار `005_workspace.sql`

لاگ `portal.preflight` با `ok: true` نشان می‌دهد تنظیمات اولیه پذیرفته شده‌اند. خطای بعدی، `MIGRATION_SOURCE_MISSING` برای `005_workspace.sql`، به تاریخچه دیتابیس مربوط است: نام این فایل در جدول `schema_migrations` ثبت شده ولی سورس آن در بسته موجود نیست.

بررسی ZIP ورودی و نسخه 1.6.0 نشان داد هیچ‌کدام این فایل را ندارند. migration شماره 005 همراه این بسته، `005_provider_base_url_from_environment.sql` است. **شباهت شماره فایل دلیل یکسان‌بودن دو migration نیست.** اسکریپت قدیمی ردیف‌های تاریخچه بدون فایل را بررسی نمی‌کرد؛ نسخه 1.6.0 بررسی سخت‌گیرانه‌تری اضافه کرد و ناسازگاری موجود را آشکار ساخت.

## رفتار اصلاح‌شده در نسخه 1.7.0

برای همین سابقه مشخص، مسیر سازگاری محدود اضافه شده است. برنامه فقط وقتی بدون اجرای SQL جدید ادامه می‌دهد که **هر هفت migration اصلی همراه بسته، با نام و SHA-256 دقیق خودشان در تاریخچه ثبت شده باشند** و ساختار فعلی دیتابیس نیز با ساختار مورد نیاز این نسخه مطابقت داشته باشد. هیچ migration اصلیِ اجرا‌نشده یا فایل تاریخی ناشناخته دیگری پذیرفته نمی‌شود.

بررسی ساختار شامل 24 جدول، 232 ستون و نوع/nullable/default آن‌ها، 107 قید کلید اصلی/خارجی/یکتایی/CHECK، 57 ایندکس، چهار enum، سه تابع و دو trigger است. فعال‌بودن محافظ ثبت وقایع مدیریت و یکتایی شناسه نوبت چت نیز بررسی می‌شود. قید یا trigger ناشناخته‌ای که نوشتن برنامه را تغییر دهد، مسیر سازگاری را متوقف می‌کند. جدول‌ها و ستون‌های اضافه صرفاً در محدوده قواعد مستند در `scripts/migration-compatibility.mjs` تحمل می‌شوند.

در حالت پذیرفته‌شده، ردیف `005_workspace.sql` با همان checksum و تاریخ اصلی حفظ می‌شود؛ هیچ ردیف تاریخچه، اطلاعات کاربر یا تنظیمات مدل‌ها تغییر نمی‌کند. لاگ زیر پیش از `migration.ready` ثبت می‌شود:

```text
migration.legacy_history_retained
sourceUnavailable: true
canonicalHistoryVerified: true
schemaVerified: true
historyChanged: false
applied: 0
```

این یک تصمیم محدود برای سازگاری ساختار موجود است؛ **به معنی بازیابی یا تأیید محتوای فایل مفقود نیست**. سیاست به SHA-256 همین هفت فایل بسته شده است و نسخه آینده با migration جدید، خودکار از این استثنا استفاده نمی‌کند. متغیر محیطی برای نادیده‌گرفتن خطاها یا خاموش‌کردن بررسی وجود ندارد.

## گزارش بدون تغییر دیتابیس

در ترمینال برنامه و با همان متغیرهای موجود اجرا کنید:

```bash
node scripts/migration-status.mjs
```

این فرمان تراکنش `READ ONLY` باز می‌کند. فقط نام و checksum فایل‌های migration و مشخصات ستون‌های چند جدول را می‌خواند؛ هیچ داده کاربر، رمز اتصال یا مقدار تنظیمات چاپ نمی‌کند. جدول نمی‌سازد، SQL migration اجرا نمی‌کند و checksum را تغییر نمی‌دهد. اگر ناسازگاری وجود داشته باشد، گزارش چاپ می‌شود و کد خروج 1 است؛ این رفتار برای گزارش خطاست.

بخش `missingSources` نام فایل اصلی و `recordedChecksum` ثبت‌شده را مشخص می‌کند. `matchingBundledSources` تنها تطابق دقیق محتوا با فایل‌های همراه را نشان می‌دهد و به‌تنهایی مجوز تغییر تاریخچه نیست. فرمان از همان سیاست بررسی زمان شروع استفاده می‌کند: در حالت پذیرفته‌شده، `compatible: true` و `legacyCompatibility.sourceUnavailable: true` است. در حالت رد، کد دقیق و در صورت اختلاف ساختار، `legacyCompatibility.schemaIssues` مشخص می‌کند کدام تعریف با قرارداد سازگار نیست.

| کد | معنی |
|---|---|
| `MIGRATION_LEGACY_REQUIRES_COMPLETE_HISTORY` | حداقل یک migration اصلی در تاریخچه ثبت نیست؛ برنامه آن را در کنار سابقه مفقود دوباره اجرا نمی‌کند |
| `MIGRATION_CHECKSUM_MISMATCH` | محتوای یکی از migrationهای اصلی با تاریخچه تفاوت دارد |
| `MIGRATION_LEGACY_SCHEMA_MISMATCH` | ساختار یا محافظ‌های دیتابیس با نسخه حاضر مطابقت ندارند |
| `MIGRATION_LEGACY_BRANCH_UNSUPPORTED` | مجموعه فایل‌های این انتشار خارج از نسخه‌ای است که سیاست سازگاری برای آن بررسی شده |
| `MIGRATION_SOURCE_MISSING` | فایل تاریخی دیگری غیر از مورد مشخص بالا موجود نیست |

## شواهد لازم برای بازیابی

اگر شرط‌های مسیر سازگاری برقرار نیستند، فایل اصلی `005_workspace.sql` را از سورس همان نسخه‌ای که قبلاً روی این دیتابیس با موفقیت اجرا شده بود بازیابی کنید. در صورت دسترسی به نسخه موفق قبلی در لیارا، ممکن است فایل در پوشه `migrations` آن وجود داشته باشد. گزارش بالا همراه فایل اصلی امکان بررسی تطابق SHA-256 و مسیر ارتقای واقعی را فراهم می‌کند. checksum به‌تنهایی برای بازسازی SQL کافی نیست.

قبل از عملیات اصلاحی از دیتابیس نسخه پشتیبان تهیه کنید. برای رفع این خطا، `schema_migrations`، ردیف تاریخی، کاربران و تنظیمات را حذف نکنید؛ checksum را با مقدار فایل متفاوت جایگزین نکنید و فایل SQL خالی یا حدسی نسازید. اجرای دوباره migrationهای قدیمی ممکن است تنظیمات فعلی مدل‌ها را بازنویسی کند؛ migration شماره 004 این پروژه شامل بازنشانی فهرست مدل‌هاست.

## آزمون‌های انجام‌شده و محدودیت تأیید

هر هفت SQL اصلی بدون تغییر، همراه `pgcrypto` روی PostgreSQL 18.3 در PGlite 0.5.8 اجرا شدند. همان کد تراکنش زمان شروع روی دیتابیس خالی و دو شروع متوالی با ردیف تاریخی مفقود آزمایش شد. تست‌ها نبود DDL/DML در مسیر سازگاری و حفظ کامل تاریخچه، کاربران و نام مدلِ ویرایش‌شده توسط مدیر را بررسی کردند. حذف migration اصلی، تغییر checksum، افزودن سابقه ناشناخته، حذف ایندکس یکتایی نوبت، خاموش‌کردن trigger حسابرسی و حذف CHECK اعتبار، همگی به توقف منجر شدند.

تعاریف catalog روی PostgreSQL 16.4 و 17.5 نیز با قرارداد یکسان مطابقت داشتند؛ نیازی به حذف تفاوت‌های معنایی یا آسان‌کردن مقایسه نبود. بسته‌های قدیمی PGlite افزونه `pgcrypto` ندارند، بنابراین در **آزمون تطبیق DDL نسخه‌های قدیمی فقط دستور نصب این افزونه کنار گذاشته شد**؛ SQL اصلی پروژه تغییر نکرد. این بخش، آزمون نصب افزونه روی سرور قدیمی لیارا نیست.

تست‌ها در `tests/migration-compatibility.test.mjs` و `tests/migration-status.test.mjs` قرار دارند. آزمون موتور SQL با `TEST_PGLITE_ROOT` اجرا می‌شود؛ این متغیر فقط برای تست محلی و مسیر بسته PGlite است و در لیارا لازم نیست. PGlite به dependencyهای برنامه اضافه نشده است. بدون این متغیر، آزمون اختیاری موتور SQL skip می‌شود و آزمون‌های مستقل سیاست اجرا می‌شوند.

سورس اصلی `005_workspace.sql` و دسترسی به دیتابیس این استقرار در زمان آماده‌سازی این نسخه موجود نبود. موفقیت مسیر سازگاری روی دیتابیس شبیه‌سازی‌شده با SQL واقعی، تأیید موفقیت ارتقای همان دیتابیس واقعی نیست. آزمون‌های PGlite نیز اتصال شبکه، TLS، نقش‌های PostgreSQL و هم‌زمانی چند اتصال واقعی لیارا را تأیید نمی‌کنند. تغییر متغیرهای محیطی یا افزایش زمان انتظار migration، شرط‌های ناسازگار تاریخچه یا ساختار را اصلاح نمی‌کند.
