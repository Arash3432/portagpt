# Portal AI — Release Notes v1.4.5

**تمرکز این نسخه: پایداری استریم پاسخ، تجربه بارگذاری، و پالایش حرکتی رابط گفتگو.**

## رفع باگ‌های گزارش‌شده

### ۱. پیغام‌های «در حال فکر کردن» (باگ گرافیکی چت)
- سه پیغام چرخشی («در حال فکر کردن…»، «دارم پاسخ را می‌چینم…»، «چند لحظه صبر کن…») به‌کلی حذف شد.
  ریشه باگ: سه برچسب روی هم چیده شده بودند و قبل از شروع انیمیشن، هر سه به‌صورت هم‌زمان و روی‌هم‌افتاده دیده می‌شدند؛ چرخه ۵.۴ ثانیه‌ای هم باعث پرش‌های سریع و آزاردهنده می‌شد.
- حالا فقط **سه نقطه‌ی متحرک آرام** نمایش داده می‌شود؛ حرکت نرم‌تر (۱.۹ ثانیه، بدون پرش) و کم‌نورتر که با حال‌وهوای مینیمال محصول هم‌خوان است.
- متن پیغام برای صفحه‌خوان‌ها (a11y) حفظ شده ولی از نمایش بصری حذف شده است.

### ۲. خطای «network error» وسط پاسخ (مهم‌ترین باگ)
ریشه‌یابی کامل و بازنویسی لایه‌ی استریم در سمت سرور و کلاینت:

**علت‌های اصلی که پیدا و رفع شد:**
- تایمر ۱۱۵ ثانیه‌ای سرور پاسخ‌های طولانی را **وسط کار قطع می‌کرد** حتی وقتی داده داشت می‌آمد؛ حالا قطع شدن فقط بر اساس **بی‌حرکتی upstream** (۷۵ ثانیه بدون هیچ بایتی) انجام می‌شود و سقف مطلق به ۲۹۰ ثانیه افزایش یافت.
- هیچ keep-alive‌ای وجود نداشت؛ مکث‌های طولانی مدل باعث قطع‌شدن اتصال در پراکسی‌ها می‌شد. حالا **هارت‌بیت** هر ۱۲ ثانیه روی اتصال می‌رود (`: ping`) و اتصال زنده می‌ماند.
- قطع‌شدن استریم به‌صورت خام (`controllerOut.error`) به مرورگر منتقل می‌شد و مرورگر خطای عمومی `TypeError: network error` می‌داد. حالا استریم همیشه **به‌صورت تمیز بسته می‌شود** و رویداد پایانی صریح دارد (`done` / `interrupted` / `error`).
- **هیچ‌چیز از پاسخ دریافتی از بین نمی‌رود:** اگر اتصال وسط پاسخ قطع شود، متنِ دریافت‌شده روی صفحه می‌ماند، در سرور هم ذخیره می‌شود (در تاریخچه باقی می‌ماند) و یک نوار کم‌رنگ با دکمه‌ی «تلاش دوباره» ظاهر می‌شود. متن پاسخ دیگر هرگز با «خطا: network error» جایگزین نمی‌شود.
- پروتکل انتقال از متن خام به **SSE استاندارد** (`text/event-stream`) تغییر کرد؛ همراه با هدرهای `x-accel-buffering: no` و `cache-control: no-store` برای جلوگیری از بافر شدن در پراکسی‌ها.
- خطاهای پیش از شروع پاسخ (کوتا، احراز هویت، قفل مدل) حالا به‌صورت پیام محلی و تمیز نمایش داده می‌شوند و حباب خالی در چت باقی نمی‌ماند.
- پاسخ خالی مدل حالا رویداد مشخص دارد، اعتبار پیام به‌صورت خودکار برمی‌گردد و کاربر پیام شفاف می‌بیند.

**دکمه‌ی توقف (Stop):**
- در حین تولید پاسخ، دکمه‌ی ارسال به **دکمه‌ی توقف** تبدیل می‌شود؛ کاربر می‌تواند هر لحظه تولید را متوقف کند. متنِ دریافت‌شده تا آن لحظه حفظ می‌شود و در سرور هم ذخیره می‌ماند.
- توقف، نظرسنجی تصویر را هم لغو می‌کند (قبلاً حلقه‌ی نظرسنجی پس از خروج از صفحه ادامه پیدا می‌کرد — نشتی تایمر؛ رفع شد).

**تلاش مجدد تمیز (بدون پیام تکراری):**
- مایگریشن `006_chat_stream_resilience.sql`: ستون `client_turn_id` روی جدول پیام‌ها. هر «نوبت» کاربر شناسه‌ی پایدار دارد؛ تلاش مجدد، پاسخ ناقص قبلی را در دیتابیس **جایگزین** می‌کند و دیگر پیام کاربر تکراری در تاریخچه ثبت نمی‌شود.

### ۳. بازکردن گفت‌وگوهای قبلی
- با کلیک روی هر گفت‌وگو، یک **صفحه‌ی اسکلتون لودینگ اختصاصی** (آواتار + حباب‌های شنیِ متحرک، راست‌چین/چپ‌چین درست) نمایش داده می‌شود؛ دیگر حس «سایت خراب شده» وجود ندارد.
- اسکلتون حداقل ۳۲۰ میلی‌ثانیه می‌ماند تا پرش ناگهانی و فلش‌زدن نداشته باشیم.
- دابل‌کلیک و کلیک‌های پیاپی محافظت شده‌اند: آیتم در حال بازشدن غیرفعال می‌شود، اسپینر کوچک می‌گیرد و درخواست‌های قبلی لغو می‌شوند (محافظت در برابر race).
- اگر بازکردن شکست بخورد، به‌جای toast ساده یک **کارت خطا با دکمه‌ی تلاش دوباره** در همان ناحیه‌ی چت نمایش داده می‌شود.
- کلیک روی گفت‌وگویی که همین حالا باز است، فقط سایدبار را می‌بندد (بدون درخواست اضافه).

## بهبودهای عمیق‌تر (گرافیکی / محیطی / تجربی / مصرف طولانی‌مدت)

- **کارایی استریم در پاسخ‌های بلند:** رندر متن دریافتی به ۹ فریم در ثانیه محدود شد (قبلاً به‌ازای هر تکه‌ی شبکه، کل پیام دوباره پارس می‌شد — O(n²) روی موبایل). اسکرول و باتری به‌طور محسوس بهتر است.
- **MutationObserver اسکرول‌ریویل** به یک فریم در هر فرصت تبدیل شد (قبلاً در هر جهش DOM، کل صفحه querySelectorAll می‌شد).
- **کلاهبرداری اسکرول مرورگر غیرفعال شد** (`overflow-anchor: none` روی ناحیه‌ی پیام‌ها) تا پرش‌های تصادفی وسط استریم حذف شود.
- **کیبورد موبایل:** در iOS ارتفاع صفحه از visualViewport دنبال می‌شود و در اندروید از `interactive-widget=resizes-content`؛ کامپوزر همیشه بالای کیبورد می‌نشیند.
- **a11y:** حذف `aria-live` از بدنه‌ی پیام در حال استریم (قبل از این، هر تکه‌ی متن برای صفحه‌خوان اعلام می‌شد)؛ caret تزئینی `aria-hidden` شد.
- **لمس:** دکمه‌ی کپی روی دستگاه‌های لمسی همیشه قابل‌دیدن است (قبلاً فقط بین ۷۲۰px به پایین).
- **رفع race جست‌وجوی تاریخچه:** پاسخ‌های قدیمی‌تر نمی‌توانند لیست جدیدتر را بازنویسی کنند.
- **خروج از حساب وسط استریم:** استریم و نظرسنجی‌ها ابتدا لغو می‌شوند.
- **افزودن متغیر تم `--amber`** برای حالت روشن/تیره (نوار «اتصال قطع شد»).

## فایل‌های تغییر یافته
- `app/api/chat/route.ts` — پروتکل SSE، هارت‌بیت، watchdog بی‌حرکتی، ذخیره‌ی پاسخ ناقص، turn id
- `app/ui/portal-app.tsx` — کلاینت SSE، دکمه‌ی توقف، تلاش مجدد، اسکلتون گفت‌وگو، محافظت‌های race
- `app/ui/portal-app.module.css` — نقطه‌های آرام، اسکلتون، نوار وقفه، دکمه‌ی توقف، `overflow-anchor`
- `app/ui/public-shell.tsx` — rAF-coalesced reveal observer
- `app/layout.tsx` — `interactiveWidget` برای کیبورد اندروید
- `app/globals.css` — متغیر `--amber`
- `migrations/006_chat_stream_resilience.sql` — مایگریشن جدید (خودکار در استارت اجرا می‌شود)

## استقرار
- مایگریشن ۰۰۶ در اجرای بعدی سرویس به‌صورت خودکار اعمال می‌شود (`npm start` → `scripts/migrate.mjs`).
- بدون تغییر نیازمندی‌های زیرساخت؛ همان ایمیج Docker و `liara.json` قبلی.
