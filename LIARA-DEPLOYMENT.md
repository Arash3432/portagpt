# استقرار Portal AI 1.7.1 در لیارا

## خروجی و شیوه اجرا

این سورس برای برنامه Docker با پورت 3000 آماده شده است. `npm run build` ابتدا Next.js را کامپایل و سپس خروجی مستقل را در `.next/standalone` آماده می‌کند. پوشه خروجی، سرور، فایل‌های عمومی، CSS/JavaScript، migrationها و اسکریپت‌های شروع را دارد. Docker از Node 22 Alpine و کاربر غیر root استفاده می‌کند؛ مرحله اجرا نصب مجدد dependency یا کامپایل TypeScript انجام نمی‌دهد.

`npm start` و `npm run start:liara` یک مسیر مشترک دارند: بررسی تنظیمات، اجرای migration، شروع سرور روی `0.0.0.0`. هر سه مرحله در یک فرایند Node اجرا می‌شوند تا سیگنال توقف میزبان به خود سرور برسد.

## تنظیمات ضروری

| متغیر | مقدار یا قاعده |
|---|---|
| `NODE_ENV` | `production` |
| `PUBLIC_APP_URL` | origin نهایی با HTTPS؛ بدون مسیر، query یا نام کاربری |
| `DATABASE_URL` | URI واقعی `postgres://` یا `postgresql://` از سرویس PostgreSQL |
| `DATABASE_SSL_MODE` | `disable` برای اتصال خصوصی بدون TLS؛ `require` برای اتصال TLS مطابق تنظیم واقعی سرویس |
| `REDIS_URL` | URI واقعی `redis://` یا `rediss://` |
| `SESSION_PEPPER` / `OTP_PEPPER` | دو راز تصادفی متفاوت، هرکدام حداقل 32 کاراکتر |
| `CONFIG_ENCRYPTION_KEY` | base64 دقیقاً 32 بایت، یا 64 کاراکتر hex |
| `ADMIN_IP_ALLOWLIST` | IP عمومی ثابت و اختصاصی مدیر؛ چند مقدار با کاما؛ خالی یعنی مدیریت غیرفعال |
| `TRUSTED_PROXY_HOPS` | تعداد proxyهای مورد اعتماد از سمت راست X-Forwarded-For، بین 1 و 5 |
| `AI_BASE_URL` / `AI_PROVIDER_HOST_ALLOWLIST` | endpoint حساب واقعی و hostnameهای صریح مجاز |

برای تصویر و فایل، چهار مقدار `S3_ENDPOINT`، `S3_BUCKET`، `S3_ACCESS_KEY_ID` و `S3_SECRET_ACCESS_KEY` باید همگی تنظیم شوند. endpoint در production باید HTTPS باشد. تنظیم ناقص S3 پیش از شروع رد می‌شود؛ اگر هیچ‌کدام وجود نداشته باشد، وب‌سایت می‌تواند شروع شود ولی قابلیت فایل و تصویر آماده نیست. bucket باید خصوصی باشد و کلید برنامه فقط به bucket لازم دسترسی داشته باشد.

پیامک اختیاری است. `OTP_DEV_CODE` در production پذیرفته نمی‌شود. `PAYMENTS_ENABLED=false` را تا آزمون کامل درگاه واقعی نگه دارید.

متغیرهای محرمانه را در Environment کنسول ثبت کنید. در `liara.json` مقدار `envs` اضافه نکنید: طبق مستندات لیارا، این فیلد می‌تواند متغیرهای قبلی برنامه را جایگزین کند. Secretها را در ZIP یا Git قرار ندهید.

## دسترسی مدیریت و proxy

حالت فعلی، IP-only است و هویت فردی مدیر را با رمز یا MFA بررسی نمی‌کند. **همه دستگاه‌هایی که از IP مجاز خارج می‌شوند، دسترسی مدیر دارند.** این شامل خروجی مشترک سازمان یا VPN عمومی می‌شود. IP یک راز محسوب نمی‌شود؛ کنترل امنیتی بر مسیر شبکه معتبر و اختصاصی متکی است.

ingress باید هدر واقعی را بازنویسی کند یا آدرس مشاهده‌شده را به زنجیره معتبر اضافه کند. برنامه نباید با مسیری قابل دورزدن ingress در اینترنت باز شود. در حضور CDN، Load Balancer یا proxy اضافی، طول زنجیره را بررسی و `TRUSTED_PROXY_HOPS` را مطابق مسیر واقعی تنظیم کنید. برای آزمون انتشار، درخواست‌هایی با X-Forwarded-For ساختگی از IP غیرمجاز نباید مدیریت را باز کنند. مقدار پیش‌فرض یک، به‌تنهایی تضمین معتبر بودن تمام توپولوژی‌های شبکه نیست.

## migration و بازیابی خطا

برای خطای `MIGRATION_SOURCE_MISSING` و فایل‌های تاریخی مانند `005_workspace.sql` یا `007_security_identity.sql`، ابتدا [راهنمای ارتقا](UPGRADE-1.7.1.md) و [شرح مسیر سازگاری](MIGRATION-RECOVERY.md) را ببینید. نسخه 1.7.1 تاریخچه شاخه‌های مختلف را با قرارداد ساختار فعلی تطبیق می‌دهد؛ checksum فایل‌های ثبت‌شده و شناخته‌شده همچنان کنترل می‌شود. متغیر جدیدی لازم نیست. گزارش اختلاف‌ها در خطای شروع خودکار ثبت می‌شود؛ ترمینال لازم نیست.

پیش از اولین انتشار و هر تغییر schema، از PostgreSQL بکاپ بگیرید. فایل‌های SQL اعمال‌شده را ویرایش نکنید؛ تغییر جدید باید فایل شماره‌دار جدید باشد.

موتور migration شماره فایل‌ها را عددی مرتب می‌کند، شماره تکراری را رد می‌کند و تمام تاریخچه checksumها را قبل از اجرای تغییر جدید بررسی می‌کند. قفل تراکنشی PostgreSQL قبل از ساخت جدول تاریخچه گرفته می‌شود، بنابراین دو instance روی اولین شروع هم‌زمان با هم برخورد نمی‌کنند. تمام تغییرهای pending و ردیف‌های تاریخچه در یک تراکنش commit می‌شوند؛ شکست باعث rollback همان مجموعه می‌شود. migrationهای جدید باید داخل تراکنش قابل اجرا باشند؛ برای مثال `CREATE INDEX CONCURRENTLY` نباید در این مسیر قرار گیرد.

`MIGRATION_WAIT_SECONDS=90` فرصت تلاش مجدد برای خطاهای موقت اتصال و قفل را تعیین می‌کند؛ بازه مجاز 10 تا 300 ثانیه است. زمان اجرای خود SQL و timeout اتصال/قفل جداست. خطای رمز دیتابیس، نبود مجوز یا SQL اشتباه با retry پنهان نمی‌شود. خروجی خطا فقط کد و در صورت امن‌بودن نام فایل را دارد؛ URI و رمز ثبت نمی‌شوند.

| کد یا رخداد | اقدام |
|---|---|
| `portal.preflight` با `ok: false` | فقط فیلدهای گزارش‌شده را در Environment اصلاح کنید؛ هیچ مقداری در لاگ چاپ نمی‌شود |
| `migration.waiting` | اتصال خصوصی، آماده‌بودن PostgreSQL و اجرای هم‌زمان انتشارها را بررسی کنید |
| `28P01` | نام کاربری/رمز PostgreSQL معتبر نیست |
| `42501` | نقش دیتابیس مجوز لازم برای migration یا extension را ندارد |
| `MIGRATION_CHECKSUM_MISMATCH` | نسخه اصلی همان فایل migration اعمال‌شده را بازیابی کنید؛ checksum دیتابیس را دست‌کاری نکنید |
| `MIGRATION_SOURCE_MISSING` | سورس و دیتابیس متعلق به نسخه‌های سازگار نیستند؛ بسته کامل درست را استفاده کنید |
| `MIGRATION_HISTORY_GAP` | یک migration قدیمی در تاریخچه ثبت نیست ولی فایل جدیدتر ثبت شده؛ از بکاپ و تاریخچه انتشار برای بررسی استفاده کنید |

در صورت ناسازگاری تاریخچه، حذف جدول `schema_migrations` یا حذف اطلاعات کاربران راه‌حل نیست. rollback برنامه هم باید با نسخه schema سازگار باشد.

## بررسی قبل و بعد از انتشار

```bash
npm ci
npm test
npm run build
npm run validate:artifact
npm run audit:production
```

در محیطی که Environment واقعی به فرایند داده شده، `npm run preflight` تنظیمات را بدون اتصال و بدون تغییر دیتابیس بررسی می‌کند. برای محیط محلی می‌توانید فایل خصوصی `.env.local` با `NODE_ENV=development` و origin محلی ایجاد کنید و فرمان‌ها را با `node --env-file=.env.local scripts/preflight.mjs` و `node --env-file=.env.local scripts/migrate.mjs` اجرا کنید.

فرمان `npm run deployment:integration` تنها با `TEST_DATABASE_URL` به یک دیتابیس آزمایشی دورریختنی اجرا می‌شود. تست‌ها schemaهای تصادفی مخصوص خود می‌سازند و حذف می‌کنند و سه حالت شروع هم‌زمان، rollback و رد checksum تغییرکرده را بررسی می‌کنند. بدون این متغیر تست‌ها **skip** می‌شوند؛ skip به معنی موفقیت اتصال واقعی نیست.

پس از بارگذاری ZIP سورس در برنامه Docker:

1. health check برنامه در `liara.json` از `/api/health` استفاده می‌کند، timeout درخواست دارد و برای شروع 120 ثانیه مهلت در نظر می‌گیرد.
2. پاسخ health باید نسخه `1.7.1` را نشان دهد؛ PostgreSQL باید سالم باشد. اگر Redis آماده نباشد، پاسخ صریحاً `status: degraded` و `redis: unavailable` دارد. نتیجه Redis را هم جداگانه بررسی کنید: HTTP 200 به‌تنهایی تأیید آماده‌بودن صف تصویر و rate limit نیست.
3. ثبت‌نام/ورود، refresh و حفظ نشست، منوی حساب و خروج از حساب را بررسی کنید.
4. از IP مجاز و غیرمجاز، ورود به مدیریت و رد هدر ساختگی را بررسی کنید.
5. یک پاسخ متنی استریم‌شده، توقف تولید و تلاش مجدد پس از قطع اتصال را بررسی کنید.
6. یک تصویر واقعی بسازید، دانلودش کنید و دسترسی همان فایل با کاربر دیگر را امتحان کنید؛ فایل خصوصی نباید در دسترس حساب دیگر باشد.
7. حالت صف/شکست تصویر، آزادسازی اعتبار، ظرفیت Redis/S3، هزینه واقعی مدل و بکاپ دیتابیس را بررسی کنید.

این راهنما گزارش استقرار واقعی در حساب لیارا نیست. build، تست‌های مستقل و اعتبارسنجی فایل باید جدا از آزمون سرویس‌های واقعی گزارش شوند؛ بدون دسترسی به PostgreSQL/Redis/S3/AI واقعی، موفقیت اتصال و انتشار نهایی قابل تأیید نیست.

## مستندات رسمی بررسی‌شده

- [استقرار سریع Docker در لیارا](https://docs.liara.ir/paas/docker/quick-start/) — برنامه Docker، بارگذاری ZIP و ساخت image در لیارا.
- [تنظیمات liara.json](https://docs.liara.ir/paas/liarajson/) — platform، port، healthCheck و رفتار envs.
- [خروجی standalone در Next.js](https://nextjs.org/docs/app/api-reference/config/next-config-js/output) — خروجی مستقل و لزوم کپی public و static.
- [Self-hosting در Next.js](https://nextjs.org/docs/app/guides/self-hosting) — reverse proxy، متغیرهای زمان اجرا و ملاحظات انتشار.

مستندات همراه نسخه نصب‌شده Next.js 16.3.4 نیز پیش از تغییر تنظیمات بررسی شد.
