# بازیابی تاریخچه و شروع برنامه در نسخه 1.7.1

خطاهای گزارش‌شده `MIGRATION_SOURCE_MISSING` برای `005_workspace.sql` و `007_security_identity.sql` به تاریخچه یک شاخه قدیمی مربوط‌اند. این فایل‌ها در سورس ارسال‌شده وجود نداشتند. نسخه 1.7.0 برای یک نام مشخص استثنا داشت؛ این تصمیم برای تاریخچه چندشاخه کافی نبود و در 1.7.1 جایگزین شد.

## سه مسیر مشخص

| وضعیت واقعی | رفتار شروع |
|---|---|
| دیتابیس خالی | اجرای هفت SQL اصلی در یک تراکنش و ثبت همان checksumها |
| تاریخچه شناخته‌شده و پیوسته با فایل جدید در انتها | اجرای فقط فایل‌های جدید؛ کنترل checksum پیش از هر تغییر |
| تاریخچه شاخه دیگر، فاصله در ثبت فایل‌ها یا ساختار موجود بدون تاریخچه | بررسی کامل ساختار فعلی و ادامه بدون اجرای SQL تاریخی، فقط در صورت سازگاری |

در مسیر سوم، نام‌هایی مثل `005_workspace.sql`، `007_security_identity.sql` یا نام معتبر دیگری از انتشار قدیمی به‌تنهایی باعث خطا نیستند. ردیف‌ها همان‌طور که هستند باقی می‌مانند. checksum هیچ فایل موجود و شناخته‌شده‌ای نادیده گرفته نمی‌شود؛ اختلاف آن، نام نامعتبر، checksum خراب یا تاریخچه تکراری همچنان مانع ادامه‌اند.

این مسیر، ثبت ساختگیِ اجرا شدن SQL نیست. اگر نام بعضی migrationهای این سورس در تاریخچه شاخه قدیمی نباشد ولی قرارداد کامل runtime برقرار باشد، برنامه در حالت `schema_baseline` اجرا می‌شود. نام آن‌ها در `unrecordedSources` گزارش می‌شود و هیچ ردیفی برایشان ساخته نمی‌شود. محتوای SQL تاریخی مفقود نیز تأیید یا بازسازی نشده است.

## قرارداد سازگاری

بررسی شامل جدول‌ها، ستون و نوع و NULL/default، قیدها، ایندکس‌ها و رفتار یکتایی NULL، enumها، تابع‌ها و triggerهای ضروری و وجود پلن پایه رایگان است. محافظ‌های مالکیت پیام، اعتبار مصرف و ثبت وقایع مدیریت جزء قرارداد هستند. نام متفاوت ایندکس یا قید با تعریف یکسان پذیرفته می‌شود، چون کد runtime آن‌ها را با نام صدا نمی‌زند. تعریف متفاوت هم‌نام خودکار پذیرفته نمی‌شود.

ستون‌های اضافی و جدول‌های مستقل می‌توانند باقی بمانند؛ قید و trigger اضافی مؤثر بر نوشتن در جدول‌های برنامه یا ستون اجباری جدید بدون default، نیازمند بررسی‌اند. برنامه آن‌ها را حذف یا تغییر نمی‌دهد. این سیاست به checksum دقیق هفت فایل همین انتشار متصل است. افزودن migration در انتشار آینده باید همراه بازبینی صریح قرارداد و مسیر ارتقا باشد؛ تغییر فایل‌ها خودکار مجوز ادامه نمی‌دهد.

در شروع موفق چنین رخدادی ثبت می‌شود:

```json
{"event":"migration.compatibility_verified","mode":"schema_baseline","schemaVerified":true,"historyChanged":false,"applied":0}
```

رخداد واقعی، نام فایل‌های تاریخی حفظ‌شده و وضعیت کامل‌بودن تاریخچه اصلی را هم دارد. سپس `migration.ready` ثبت و سرور شروع می‌شود.

## بدون اتصال به ترمینال

اگر اختلاف ساختاری یا checksum مانع شروع باشد، برنامه خودش یک گزارش فقط‌خواندنی می‌گیرد و به‌صورت رخدادهای کوچک در لاگ می‌نویسد:

- `migration.report.summary`: تعداد اختلاف‌ها و کد اصلی؛
- `migration.report.source`: وضعیت و hash فایل‌های این سورس؛
- `migration.report.external_source`: همه فایل‌های تاریخی فاقد سورس؛
- `migration.report.history_issue`: همه اختلاف‌های تاریخچه؛
- `migration.report.schema_issue`: همه اختلاف‌های ساختار؛
- `migration.report.complete`: پایان گزارش.

گزارش فقط metadata را دارد؛ اطلاعات کاربران، رمز اتصال، مقادیر تنظیمات، SQL و بدنه تابع‌ها چاپ نمی‌شوند. دستور دستی `node scripts/migration-status.mjs` همچنان موجود است، اما برای خطای شروع لازم نیست آن را اجرا کنید. خطای اتصال یا Environment باعث تلاش اضافی گزارش‌گیری نمی‌شود.

اگر ساختار یا checksum واقعاً متفاوت باشد، ادامه خودکار برنامه قابل تأیید نیست؛ گزارش بالا مبنای اصلاح بعدی است. حذف `schema_migrations`، ساخت SQL خالی با نام مفقود یا جایگزین کردن checksum با مقدار حدسی راه بازیابی نیست. migration قدیمی 004 شامل بازنشانی فهرست مدل‌هاست و نباید روی دیتابیس موجود دوباره اجرا شود.

## آزمون و حدود آن

خود اسکریپت شروع production از طریق اتصال TCP PostgreSQL آزمایش شد: نسخه قبلی روی همان fixture خطای `007_security_identity.sql` داد؛ نسخه اصلاح‌شده دو بار شروع شد و تاریخچه، کاربران و تنظیم مدل ثابت ماندند. نقص هم‌زمان ستون و trigger نیز به گزارش خودکار کامل و توقف منجر شد. قرارداد روی PostgreSQL 16.4 و 17.5 هم مقایسه شد. جزئیات در `VALIDATION-1.7.1.md` است.

دیتابیس واقعی لیارا و SQL تاریخی مفقود در اختیار نبودند. این شواهد، تأیید آزمون روی همان حساب و همان داده‌های واقعی نیستند.
