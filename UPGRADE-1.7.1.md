# انتشار اصلاحیه 1.7.1 در لیارا

**نیازی به ترمینال یا متغیر محیطی جدید ندارید.** خطای فونت Yekan در مرورگر، متعلق به صفحه 502 لیارا است. توقف اصلی در لاگ برنامه، هنگام بررسی تاریخچه دیتابیس رخ می‌داد.

1. از دیتابیس فعلی بکاپ بگیرید و همان برنامه، PostgreSQL، Redis و فضای فایل را حفظ کنید.
2. ZIP این نسخه را به‌عنوان **انتشار جدید** همان برنامه Docker با پورت 3000 بارگذاری کنید. `Dockerfile` و `liara.json` در ریشه ZIP قرار دارند؛ سورس را با پوشه نسخه‌های قبل مخلوط نکنید. restart کردن نسخه قبلی کافی نیست.
3. Environment قبلی را تغییر ندهید. `CONFIG_ENCRYPTION_KEY`، `SESSION_PEPPER` و `OTP_PEPPER` باید همان مقدارهای قبلی بمانند. به تولید دوباره Secret یا ثبت دوباره کلید AI نیازی نیست.
4. در شروع سازگار، لاگ `migration.compatibility_verified` و سپس `migration.ready` را می‌بینید. `/api/health` نسخه `1.7.1` را نشان می‌دهد. اگر Redis موقتاً آماده نباشد، وضعیت `degraded` و `redis: unavailable` صریح نمایش داده می‌شود؛ آماده‌بودن فایل/تصویر و محدودسازی درخواست همچنان به Redis وابسته است.
5. اگر ساختار دیتابیس واقعاً ناسازگار باشد، خود برنامه تمام گزارش را بین `migration.report.summary` و `migration.report.complete` در لاگ می‌نویسد. همان بخش کافی است؛ لازم نیست به ترمینال برنامه خراب متصل شوید. هیچ رمز، اطلاعات کاربران یا متن SQL در این گزارش چاپ نمی‌شود.

اصلاح این نسخه، افزودن یک فایل SQL حدسی یا حذف رکوردهای دیتابیس نیست. نام فایل‌های قدیمی به‌تنهایی باعث توقف نمی‌شود؛ ساختار مورد نیاز برنامه بررسی می‌شود و در مسیر سازگاری هیچ SQL تاریخی دوباره اجرا نمی‌شود. گزارش‌های متفاوتِ ساختار یا checksum، همچنان برای حفظ اطلاعات باعث توقف می‌شوند و جزئیاتشان خودکار ثبت می‌شود.

در محیط آزمایش، نسخه 1.7.0 روی دیتابیس دارای فایل‌های تاریخی گزارش‌شده با همان خطا متوقف شد. نسخه 1.7.1 روی همان دیتابیس با اجرای واقعی `scripts/start.mjs` و اتصال TCP PostgreSQL، دو بار بالا آمد. این شواهد مربوط به آزمون است؛ دسترسی به دیتابیس واقعی حساب لیارا در اختیار نبود.

جزئیات: [MIGRATION-RECOVERY.md](MIGRATION-RECOVERY.md) · [VALIDATION-1.7.1.md](VALIDATION-1.7.1.md).
