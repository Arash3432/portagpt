# Portal AI 1.7.1

- جایگزینی بررسی موردی نام `005_workspace.sql` با بررسی تاریخچه و ساختار برای تمام فایل‌های تاریخیِ دارای قالب معتبر پروژه؛ `007_security_identity.sql` و نام‌های دیگر نیز از همان مسیر عبور می‌کنند.
- پذیرش ساختار موجود و سازگار در شاخه‌هایی با نام یا ثبت متفاوت migration، بدون درج checksum ساختگی، حذف سابقه یا اجرای دوباره SQL.
- کنترل همه checksumهای شناخته‌شده، قالب تاریخچه، محافظ‌های مالکیت و سهمیه، تابع‌ها، triggerها، ایندکس‌ها و ردیف پایه پلن رایگان.
- تشخیص ایندکس و قید هم‌معنا با نام متفاوت، همراه کنترل رفتار NULL در یکتایی.
- گزارش خودکار و چندخطی تمام اختلاف‌ها در خطای شروع؛ نیاز به اتصال ترمینال برای دریافت گزارش از بین رفت.
- اصلاح health check هنگام اتصال مجدد Redis؛ بررسی آن محدود به زمان است و وضعیت خرابی با `degraded` گزارش می‌شود. خطای دیتابیس همچنان پاسخ 503 می‌دهد و محدودسازی امنیتی درخواست‌ها دور زده نمی‌شود.
- افزودن آزمون راه‌اندازی خودِ سرور standalone با اتصال PostgreSQL TCP و مقایسه با رفتار نسخه قبلی.
- رابط کاربری، دانلود تصویر، اطلاعات کاربران، تنظیمات ارائه‌دهنده و متغیرهای محیطی نسخه 1.7.0 حفظ شده‌اند. dependency جدیدی به برنامه اضافه نشده است.

برای انتشار [UPGRADE-1.7.1.md](UPGRADE-1.7.1.md) را بخوانید.
