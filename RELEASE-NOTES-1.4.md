# Portal AI 1.4 — نسخهٔ پایدارسازی و آماده‌سازی انتشار

## تجربهٔ کاربر و حساب

- وضعیت نشست به‌صورت مشترک و مقاوم در صفحه‌های عمومی، فضای کار، حساب و تنظیمات خوانده می‌شود.
- تا وقتی وضعیت ورود در حال بررسی است، فرم ورود/ثبت‌نام خودکار باز نمی‌شود.
- کاربر واردشده با دکمه‌های «شروع»، «ورود» و CTAهای صفحهٔ اصلی مستقیم به فضای کار می‌رود؛ فرم ثبت‌نام دوباره نمایش داده نمی‌شود.
- تغییر ورود یا خروج در تب دیگر با رویداد محلی و بازبینی هنگام بازگشت به تب همگام می‌شود.
- اختلال موقت سرویس نشست به‌عنوان «خارج‌شدن» تفسیر نمی‌شود؛ رابط وضعیت قابل‌تلاش‌مجدد نشان می‌دهد.
- انتخاب پلن برای کاربر واردشده به‌صورت امن در صفحهٔ پلن‌ها باقی می‌ماند، کارت انتخاب‌شده را برجسته می‌کند و صریحاً می‌گوید پرداخت/فعال‌سازی انجام نشده است.

## پنل مدیریت

- خطای ۵۰۳ نشست دیگر به‌اشتباه «این حساب مدیر نیست» نمایش داده نمی‌شود.
- خطای بارگذاری یک بخش از داده‌ها، دسترسی تأییدشدهٔ مدیر را پاک نمی‌کند.
- خروج ناموفق وضعیت پنل را پاک نمی‌کند؛ پس از خروج موفق داده‌های حساس cache‌شده پاک می‌شوند.
- درخواست‌های قدیمی جست‌وجوی کاربر یا Drawer جزئیات نمی‌توانند روی پاسخ جدیدتر بنویسند.
- فوکوس Dialogهای ورود و حذف حساب پایدار شده و هنگام تایپ از بین نمی‌رود.

## احراز هویت و امنیت

- شمارهٔ `ADMIN_PHONE_E164` دیگر با ثبت‌نام یا OTP به نقش مدیر ارتقا پیدا نمی‌کند.
- اولین ورود مدیر فقط با شمارهٔ مدیر و `ADMIN_PASSWORD` انجام می‌شود؛ همان ورود حساب مدیر را bootstrap می‌کند و رمز را به‌صورت hash ذخیره می‌کند.
- `ADMIN_IP_ALLOWLIST` واقعاً اختیاری است: اگر خالی باشد ورود مدیر با سایر کنترل‌های محافظتی کار می‌کند؛ اگر تنظیم شود دقیقاً اعمال می‌شود.
- مقایسهٔ رمز bootstrap و CSRF حساس به زمان شده و کوکی نشست malformed با بخش اضافی رد می‌شود.
- بازنشانی مسیرهای fetch خارجی برای chat، صف تصویر و SMS ممنوع شده تا redirect به مقصد ناامن رخ ندهد.
- URLهای Provider فقط با hostname عمومی و allowlist سرور پذیرفته می‌شوند؛ IP literal و مقصد داخلی رد می‌شوند.
- Service Worker فقط فایل‌های عمومیِ امن را cache می‌کند؛ پاسخ خصوصی، دارای Cookie یا `no-store` cache نمی‌شود. Asset خصوصی هم `no-store` است.

## کیفیت رابط

- ناوبری، CTAها، کارت‌های پلن، فضای کار، پنل مدیریت، فرم‌ها و حالت‌های خطا در یک زبان بصری آرام‌تر و با فاصله/سلسله‌مراتب روشن‌تر هماهنگ شده‌اند.
- حالت‌های موبایل، تبلت، دسکتاپ، کاهش حرکت و focus-visible برای تعامل قابل‌اعتمادتر تنظیم شده‌اند.
- انیمیشن‌ها کوتاه و کاربردی‌اند و برای کاربرانی که کاهش حرکت را انتخاب کنند تقریباً حذف می‌شوند.

## اعتبارسنجی انجام‌شده

- `npm ci --ignore-scripts --no-audit --no-fund`
- `npm test` — ۲۴ آزمون امنیتی/رگرسیون، TypeScript و ESLint
- `npm run audit:production` و `npm audit --audit-level=low` — بدون آسیب‌پذیری گزارش‌شده پس از به‌روزرسانی lockfile
- build production با Node 22.13 با موفقیت compile شد، `.next/BUILD_ID` تولید شد و `npm run validate:artifact` پاس شد؛ smoke test پنج صفحهٔ عمومی production نیز ۲۰۰ برگشت داد.

جزئیات دقیق، محدودیت آزمون build در محیط فعلی و چک‌لیست Liara در `SECURITY-VALIDATION-1.4.md` و `LIARA-PREDEPLOY-CHECKLIST.md` آمده است.
