# ارتقا به Portal AI 1.7.0 در لیارا

**برای این نسخه متغیر محیطی جدیدی لازم نیست.** در لاگ ارسال‌شده، `portal.preflight` موفق بود؛ علت توقف، نام یک migration قدیمی در سابقه دیتابیس است. تغییر Secret یا افزایش زمان انتظار این مشکل را حل نمی‌کند.

## برای برنامه‌ای که از قبل ساخته‌اید

1. از PostgreSQL فعلی بکاپ بگیرید. همان برنامه، دیتابیس، Redis و فضای فایل قبلی را حفظ کنید.
2. ZIP این نسخه را به‌عنوان سورس انتشار جدید همان برنامه Docker با پورت 3000 بارگذاری کنید. داخل ZIP، `Dockerfile`، `liara.json` و `package.json` مستقیماً در ریشه هستند. فایل را با پوشه‌های نسخه قبل مخلوط نکنید.
3. Environment قبلی را نگه دارید. `SESSION_PEPPER`، `OTP_PEPPER` و به‌خصوص `CONFIG_ENCRYPTION_KEY` را دوباره تولید نکنید. لازم نیست کلید AI را دوباره ثبت کنید. این بسته فیلد `envs` در `liara.json` ندارد.
4. انتشار باید سورس جدید را بسازد؛ restart کردن همان image قبلی کافی نیست. لاگ `portal-ai@1.7.0` مشخص می‌کند نسخه جدید اجرا شده است.
5. پس از `portal.preflight` باید `migration.ready` دیده شود. اگر سابقه قدیمی با ساختار این نسخه سازگار باشد، رخداد `migration.legacy_history_retained` هم ظاهر می‌شود؛ این رخداد اطلاع‌رسانی است و اطلاعات قبلی حفظ شده‌اند.
6. `/api/health` باید نسخه `1.7.0` و سلامت دیتابیس را نشان دهد. ورود، ارسال/توقف متن، ساخت تصویر و دکمه دانلود آن را با حساب خودتان امتحان کنید.

## شرط رفع خطای `005_workspace.sql`

نسخه 1.6.0 با بررسی سخت‌گیرانه‌تر تاریخچه، این رکورد قدیمی را رد می‌کرد. نسخه 1.7.0 برای همین مورد مسیر سازگاری دارد: **هر هفت migration اصلی باید با checksum درست ثبت شده باشند و ساختار دیتابیس تأیید شود.** در این حالت هیچ SQL قدیمی دوباره اجرا نمی‌شود و سابقه دیتابیس تغییر نمی‌کند.

از روی لاگ به‌تنهایی نمی‌توان تأیید کرد دیتابیس شما هر دو شرط را دارد. اگر انتشار با کد دیگری مانند `MIGRATION_LEGACY_REQUIRES_COMPLETE_HISTORY` یا `MIGRATION_LEGACY_SCHEMA_MISMATCH` متوقف شد، در ترمینال محیط برنامه اجرا کنید:

```bash
node scripts/migration-status.mjs
```

این فرمان فقط گزارش می‌گیرد و دیتابیس را تغییر نمی‌دهد. خروجی گزارش و لاگ جدید، مرحله بعدی بررسی را مشخص می‌کنند. اگر به ترمینال دسترسی ندارید، همان لاگ جدید و در صورت دسترسی، فایل `005_workspace.sql` از نسخه موفق قدیمی را ارائه کنید. نیازی به ارسال رمز یا کلید سرویس‌ها نیست.

**جدول `schema_migrations` یا ردیف‌هایش را حذف نکنید و فایل SQL خالی با نام مفقود نسازید.** جزئیات رفتار و شواهد آزمون در [MIGRATION-RECOVERY.md](MIGRATION-RECOVERY.md) آمده است.

## چه چیزهایی ثابت مانده‌اند؟

نام متغیرهای محیطی، الگوریتم رمزگذاری تنظیمات، سرویس‌های اتصال و هر هفت فایل SQL اصلی تغییر نکرده‌اند. تغییر اصلی این انتشار، رابط کاربری و رفتارهای آن و رفع محدودِ ناسازگاری شروع است. سورس برای ساخت production آماده و آزموده شده، اما انتشار داخل حساب لیارا و اتصال به سرویس‌های واقعی شما از این محیط انجام نشده است.
