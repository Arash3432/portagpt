# Portal AI — Release Notes v1.5.1

**تمرکز این نسخه: سخت‌سازی کامل زنجیره استقرار روی Liara — حذف تمام خطاهای شناخته‌شدهٔ Deploy.**
هیچ تغییر رابط کاربری یا API وجود ندارد؛ این نسخه صرفاً استقرار را مطمئن، قابل‌حذف‌خطا و قابل‌عیب‌یابی می‌کند.

## ۱. مایگریشن خودترمیم هنگام راه‌اندازی (رفع شایع‌ترین خطای Deploy)

- روی Liara، کانتینر برنامه و دیتابیس تقریباً همزمان بالا می‌آیند؛ اگر اپلیکیشن زودتر از PostgreSQL بوت شود، اسکریپت مایگریشن قبلاً بلافاصله Crash می‌کرد و کل Deploy شکست می‌خورد.
- `scripts/migrate.mjs` حالا تا **۱۲ بار با فاصله ۵ ثانیه** تلاش می‌کند و در هر تلاش یک لاگ شفاف می‌نویسد؛ مثال:
  `[portal-ai] database not ready (attempt 2/12): connect ECONNREFUSED — retrying in 5s`
- خطاهای اتصال → تلاش مجدد؛ خطاهای واقعی SQL (مثل تغییر چک‌سام مایگریشن اعمال‌شده) → توقف فوری با پیام دقیق، تا هرگز خرابی خاموش نداشته باشیم.
- هر مایگریشن اعمال‌شده هم جداگانه لاگ می‌شود: `[portal-ai] migration applied: 007_username.sql` — در داشبورد Liara دقیقاً می‌بینید چه اتفاقی افتاده.

## ۲. Health Check منصفانه‌تر — Deploy به‌خاطر یک لغزش لحظه‌ای Redis شکست نمی‌خورد

- `/api/health` قبلاً اگر Redis یک لحظه در دسترس نبود، 503 برمی‌گرداند و Liara کل Deploy را ناسالم اعلام می‌کرد.
- حالا: **دیتابیس → حیاتی** (قطعی = 503)، **Redis → غیرحیاتی** (وضعیت واقعی در بدنه پاسخ گزارش می‌شود: `{"status":"ok","database":"ok","redis":"ok"|"unavailable"}`). کلاینت Redis خودش در پس‌زمینه دوباره وصل می‌شود و Rate-limitها به‌محض بازگشت سرویس فعال می‌شوند.

## ۳. پیام خطای شفاف برای نادرست‌بودن دامنه (۴۰۳ به‌جای ۵۰۳ مبهم)

- مسیر ثبت‌نام مثل مسیر ورود، حالا اگر `Origin` مرورگر با `PUBLIC_APP_URL` نخواند، **کد 403 با پیام صریح** «دامنه فعلی با PUBLIC_APP_URL یکسان نیست» برمی‌گرداند — نه یک 503 عمومی.
- خطاهای غیرمنتظرهٔ ثبت‌نام دیگر **بی‌صدا قورت داده نمی‌شوند**؛ با `[portal-ai] register failed: ...` در لاگ Liara ثبت می‌شوند تا عیب‌یابی از راه دور ممکن باشد.

## ۴. جلوگیری از خطاهای Build ناشی از فولدرهای مجاور

- `next.config.ts` حالا `outputFileTracingRoot` را صریحاً روی ریشه پروژه پین می‌کند؛ اگر سورس را کنار پروژه دیگری (با `package-lock.json` جدا) استخراج کنید، Next.js دیگر هرگز ریشه اشتباه را حدس نمی‌زند و هشدار «multiple lockfiles» هم حذف می‌شود.
- `.dockerignore` کامل شد: دیتای تست محلی `.pgtest` (۱۵۲ مگابایت!)، فولدر `download`، لاگ‌ها و زباله‌های محلی دیگر هرگز وارد کانتکست Build Liara نمی‌شوند — آپلود سریع‌تر و Build قابل‌اعتمادتر.
- `eslint.config.mjs` هم همین پوشه‌ها را نادیده می‌گیرد تا `npm run lint` همیشه تمیز باشد.

## ۵. لاگ شفاف هنگام بوت کانتینر

- `CMD` داکرفایل حالا هر مرحله را جدا لاگ می‌کند:
  `[portal-ai] applying database migrations...` → `[portal-ai] migrations done — starting web server on port 3000...`
- در داشبورد Liara یک‌نگاهی مشخص است کجا هستید.

## استقرار (Deploy) — مسیر تأییدشده

این نسخه به‌صورت کامل به‌طور محلی با **شبیه‌سازی دقیق runtime لیارا** تست شده است:

- `npm ci` با lockfile قفل‌شده ✓ (همان مرحله اول Dockerfile)
- `next build` **با و بدون متغیرهای محیطی** ✓ (کانتینر Build لیارا هر دو حالت را می‌پذیرد)
- اجرای ۷ مایگریشن روی دیتابیس صفر (اولین استقرار) ✓
- `next start` فقط با وابستگی‌های production (دقیقاً مانند stage آخر Dockerfile) ✓
- ۱۲ سناریوی E2E روی سرور production: health، صفحه اصلی، ثبت‌نام → نام کاربری تصادفی `UserXXXXX`، نشست، تغییر نام، رد نام‌های غیرمجاز، پلن‌ها، خروج و ابطال نشست ✓
- typecheck و ESLint تمیز و **۲۵/۲۵ تست امنیتی** ✓

### گام‌های Deploy روی Liara

1. در پنل لیارا سرویس PostgreSQL و Redis را بسازید (اگر ندارید) و متغیرهای `DATABASE_URL` و `REDIS_URL` را در Environment سرویس بگذارید.
2. مقادیر `SESSION_PEPPER`، `OTP_PEPPER` (حداقل ۳۲ نویسه، با `npm run secrets:generate`)، `PUBLIC_APP_URL` (origin نهایی HTTPS) و بقیه متغیرها را طبق `.env.example` تنظیم کنید.
3. سورس نسخه 1.5.1 را آپلود و Deploy کنید؛ مایگریشن‌ها **خودکار** در بوت اجرا می‌شوند و اگر دیتابیس چند لحظه دیر آماده شود، اسکریپت خودش منتظر می‌ماند.
4. بعد از Deploy، `/api/health` باید `{"status":"ok"}` بدهد؛ اگر `redis:"unavailable"` بود، فقط آدرس Redis را چک کنید — سایت و دیتابیس سالم‌اند.

- **بدون Breaking Change**: هیچ تغییری در APIها، دیتابیس یا رابط کاربری نسبت به 1.5.0 نیست؛ ارتقا = جایگزینی سورس و Deploy مجدد.
- اگر از 1.5.0 ارتقا می‌دهید، مایگریشن‌های قبلی با چک‌سام تأیید و رد می‌شوند (nothing to do) و فقط سرویس با کد جدید بالا می‌آید.
