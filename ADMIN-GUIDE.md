# راهنمای کامل پنل مدیریت Portal AI

این راهنما برای کسی نوشته شده که برنامه‌نویسی نمی‌داند. کلید API، رمز یا خروجی دستور ساخت Secret را برای هیچ‌کس نفرستید.

## ۱) فعال‌کردن دسترسی IP-only در لیارا

در پنل لیارا وارد برنامه Portal AI شوید و بخش `Environment / Secrets` را باز کنید. در حالت فعلی فقط این متغیر برای بازکردن پنل لازم است:

```text
ADMIN_IP_ALLOWLIST=YOUR_PUBLIC_IP
```

`YOUR_PUBLIC_IP` باید IP عمومی خروجی همان دستگاه یا VPNای باشد که با آن `/admin` را باز می‌کنی؛ IP داخلی مثل `192.168.x.x` قابل استفاده نیست. چند IP را با کاما جدا کن:

در این حالت اگر `ADMIN_IP_ALLOWLIST` حذف یا خالی شود، `/admin` عمداً برای همه 404 می‌شود. مقدار `*`، `********` و wildcard معتبر نیستند.

```text
ADMIN_IP_ALLOWLIST=203.0.113.10,198.51.100.7
```

`TRUSTED_PROXY_HOPS=1` فقط برای یک ingress مورد اعتماد درست است. اگر CDN یا proxy اضافه وجود دارد، تعداد hopها از سمت راست X-Forwarded-For را با مسیر واقعی تطبیق دهید. بعد از هر تغییر Environment، برنامه را Redeploy کن. اگر IP اینترنت یا VPN تغییر کند، باید allowlist را به‌روزرسانی کنی.

این متغیرها دیگر برای پنل لازم نیستند و می‌توانی حذفشان کنی:

```text
ADMIN_PHONE_E164
ADMIN_PASSWORD
ADMIN_PASSWORD_RESET
ADMIN_SESSION_BIND_IP
```

## ۲) بازکردن پنل مدیریت

1. نشانی `https://دامنه-شما/admin` را باز کنید.
2. اگر IP فعلی در allowlist باشد، پنل مستقیماً باز می‌شود و نیازی به شماره، رمز، OTP یا کوکی ورود ندارد.
3. اگر IP مجاز نباشد، خود `/admin` و APIهای مدیریت پاسخ 404 می‌دهند.


## ۳) اتصال Liara AI

1. در منوی پنل، «اتصال Liara AI» را باز کنید.
2. کلید API را فقط در کادر «کلید API جدید» وارد کنید.
3. «ذخیره رمزگذاری‌شده» را بزنید.
4. «تست مدل‌ها و اتصال» را اجرا کنید.
5. وضعیت باید `healthy` یا دست‌کم `partial` همراه با تعداد مدل باشد.

Base URL از متغیر `AI_BASE_URL` در پنل Liara خوانده می‌شود و فقط hostnameهای `AI_PROVIDER_HOST_ALLOWLIST` را می‌پذیرد. این دو متغیر باید با هم هماهنگ باشند. برای تنظیمی که فرستادی:

```text
AI_BASE_URL=https://api.gapgpt.app/v1
AI_PROVIDER_HOST_ALLOWLIST=api.gapgpt.app
```

اگر از endpoint رسمی Liara AI استفاده می‌کنی، به‌جای آن این جفت را بگذار:

```text
AI_BASE_URL=https://ai.liara.ir/api/6a8891dfffbd76a99cabcca4/v1
AI_PROVIDER_HOST_ALLOWLIST=ai.liara.ir
```

در allowlist فقط hostname بنویس؛ `https://`، مسیر، `/v1` و wildcard ننویس.

کلید ذخیره‌شده دوباره در مرورگر نمایش داده نمی‌شود. برای تعویض آن، کلید جدید را وارد و ذخیره کنید.

## ۴) مدیریت اعتبار پلن‌ها

در «پلن و سهمیه» برای هر اشتراک این موارد مستقل‌اند:

- اعتبار پیام در هر ۳ ساعت؛
- اعتبار پیام در هر هفته؛
- اعتبار ساخت تصویر در هر هفته؛
- تعداد فایل در هر پیام و قیمت اشتراک.

پس از تغییر، «ذخیره پلن» را بزنید. سقف مالی داخلی داخل بخش بازشونده فقط محافظ هزینه کل سیستم است و سهمیه‌ای نیست که کاربر می‌بیند.

## ۵) مدیریت مدل‌ها

در «مدل‌ها» هر کارت شامل شناسه Liara AI، پلن لازم، قیمت تقریبی و اعتبار هر درخواست است.

- «اعتبار هر پیام» برای مدل متنی و «اعتبار هر تصویر» برای مدل تصویری قابل تغییر است.
- مقدار پیش‌فرض یک است، اما مدل گران‌تر می‌تواند دو یا چند اعتبار مصرف کند.
- بعد از هر تغییر «ذخیره» را بزنید.
- Sirius همیشه فعال می‌ماند و می‌تواند یک مدل جایگزین کنترل‌شده داشته باشد.
- قیمت‌ها را پس از مقایسه با صورتحساب واقعی Liara AI اصلاح کنید؛ قیمت اولیه فقط تخمین محافظه‌کارانه است.

## ۶) عملیات حساس

در حالت IP-only، عملیات مدیریتی فقط پس از بررسی IP allowlist و مبدأ مجاز درخواست اجرا می‌شوند. برای امنیت بیشتر، پنل را فقط از یک IP ثابت یا VPN با IP خروجی ثابت باز کن؛ تغییر IP باعث 404 شدن پنل می‌شود.

## ۷) کارهای روزانه مدیر

- «نمای کلی»: هزینه امروز، کاربران، فروش و سلامت اتصال؛
- «کاربران»: جستجو با شماره/ایمیل، مشاهده جزئیات، تعلیق، خروج دستگاه‌ها و تغییر پلن؛
- «امنیت و حسابرسی»: رخدادهای باز و تمام تغییرهای مدیران؛
- «کنترل هزینه»: سقف کل روزانه و توقف اضطراری سرویس.

پرداخت را فقط پس از تست درگاه، اتصال AI و بررسی قیمت واقعی مدل‌ها فعال کنید.

## ۸) خطاهای رایج

### پنل با IP ثابت باز نمی‌شود

IP واقعی خروجی دستگاه یا VPN را در `ADMIN_IP_ALLOWLIST` قرار بده، نه IP داخلی شبکه. در allowlist فقط IP معتبر بنویس؛ مقدار `*`، `********` یا placeholder عمداً رد می‌شود. اگر مقدار Environment را تازه تغییر داده‌ای، Redeploy لازم است. برای دیدن IP فعلی می‌توانی از همان شبکه یک سرویس نمایش IP عمومی را بررسی کنی.

### تست Liara AI شکست می‌خورد

کلید را در پنل Liara بررسی یا تعویض کنید، سپس دوباره ذخیره و تست کنید. اگر از GapGPT استفاده می‌کنی، هماهنگ‌بودن `AI_BASE_URL` و `AI_PROVIDER_HOST_ALLOWLIST` را بررسی کن؛ برای نمونهٔ بالا hostname باید دقیقاً `api.gapgpt.app` باشد.

### مدل پاسخ نمی‌دهد

ابتدا «تست مدل‌ها و اتصال» را اجرا کنید. سپس شناسه مدل و فعال‌بودنش را در بخش «مدل‌ها» بررسی کنید. اگر فقط یک مدل خطا دارد، همان مدل را موقتاً خاموش کنید.

### هزینه غیرعادی شد

در «کنترل هزینه» توقف اضطراری را بزنید، کلید را در Liara AI تعویض کنید، Audit Log و مصرف مدل‌ها را بررسی کنید و بعد با سقف روزانه پایین سرویس را دوباره روشن کنید.

## ۹) روال امن پیشنهادی

IP راز محسوب نمی‌شود. این مدل هویت فردی مدیر را با رمز یا MFA بررسی نمی‌کند؛ هر فردی پشت IP مجاز هم دسترسی مدیر دارد. از IP خروجی اختصاصی و مسیر کنترل‌شده استفاده کنید؛ VPN عمومی یا NAT مشترک مناسب نیست. ingress باید هدرهای آدرس را معتبر کند و خود برنامه از مسیر قابل دورزدن ingress در دسترس نباشد. روش بررسی انتشار در `LIARA-DEPLOYMENT.md` آمده است.
