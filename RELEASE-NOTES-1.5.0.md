# Portal AI — Release Notes v1.5.0

**تمرکز این نسخه: بازطراحی بزرگ تجربه کاربری — هویت کاربر، هاب حساب، و ساده‌سازی رابط.**
این نسخه بخش قابل‌توجهی از رابط را به سبک جدید (مینیمال، حرفه‌ای، محوِ جزئیات اضافه) منتقل می‌کند.

## ۱. سیستم نام کاربری (جایگزین شماره موبایل)

- دیگر نیازی نیست شماره موبایل بلند و زشت در رابط نمایش داده شود. هر کاربر یک **شناسه کوتاه و خوانا** می‌گیرد؛ مثال: `User25255` (کلمه User + ۵ رقم تصادفی).
- مایگریشن `007_username.sql`: ستون `users.username` (حداکثر ۳۲ کاراکتر)، ایندکس یکتای بدون حساسیت به بزرگی/کوچکی حروف، و **Backfill خودکار** برای همه کاربران فعلی — هیچ کاربری بدون نام کاربری نمی‌ماند و در صورت تصادم، تولید نام تا ۱۲ بار با پسوند طولانی‌تر تکرار می‌شود.
- `lib/username.ts`: اعتبارسنجی سخت‌گیرانه (۳ تا ۲۴ کاراکتر، شروع با حرف لاتین، فقط حرف/رقم/زیرخط)، تولید یکتا، و **لیست نام‌های رزرو** (admin، support، portal و…) تا کسی هویت سیستم را جعل نکند.
- ثبت‌نام: نام کاربری **داخل همان تراکنش** ساخته می‌شود و در پاسخ ثبت‌نام به کاربر نشان داده می‌شود.
- تغییر نام کاربری: endpoint جدید `PATCH /api/account/profile` با محافظت CSRF، اعتبارسنجی Zod، محدودیت نرخ (۵ بار در ساعت)، پیام خطای صریح «این نام گرفته شده»، و ثبت رویداد در `security_events` برای حسابرسی.

## ۲. هاب حساب (Account Hub) — پاپ‌آور به‌جای صفحه تمام‌صفحه

کلیک روی حساب کاربر دیگر یک صفحه بزرگ آزاردهنده باز نمی‌کند؛ یک **کشوی کناری جمع‌وجور** در همان‌جا باز می‌شود:

- **هدر هویت:** حرف آواتار + نام کاربری + بج رنگی پلن + دو نوار مینیمال مصرف کووتا.
- **پنج آیتم منو**، هرکدام یک دیالوگ وسط‌چین و بزرگ‌تر با محتوای اختصاصی:
  - **پروفایل:** ویرایش نام کاربری به‌صورت درجا (Inline) با اعتبارسنجی زنده.
  - **اشتراک و پلن‌ها:** سه سنجه مصرف + دکمه ارتقا.
  - **ظاهر:** تم (روشن/تیره)، تراکم، حرکت، رفتار دکمه Enter، پیشنهادها — همه با اعمال فوری.
  - **زبان:** شبکه‌ای از ۴ زبان (فارسی/انگلیسی/عربی/چینی) با تغییر جهت خودکار RTL/LTR.
  - **بیشتر:** میانبرهای کیبورد + دسترسی ادمین.

## ۳. نوار بالای ساده‌شده

- دکمه‌های **تغییر تم** و **تغییر زبان** از نوار بالا **حذف شدند** — هر دو به هاب حساب منتقل شدند تا نوار بالا فقط کاری‌ها را نگه دارد.
- چیپ حساب کاربر جایگزین آن دو دکمه شد و خروج از حساب هم داخل هاب قرار گرفت.

## ۴. کارت کاربر در سایدبار (جایگزین باکس اشتراک)

- باکس قبلی که فقط «اشتراک + کووتا» نشان می‌داد، به یک **کارت کاربر واقعی** تبدیل شد: حرف آواتار + نام کاربری + نوار مینی کووتا + **بج رنگی پلن** (رنگ اختصاصی برای Free / Starter / Plus / Pro / Ultra).
- حالت مهمان هم طراحی شده: کلیک روی کارت، دیالوگ ورود/ثبت‌نام را باز می‌کند.
- منوی سایدبار فقط سه مقصد تمیز نگه داشته شد: استودیو، مدل‌ها، پلن‌ها.

## ۵. موبایل

- در عرض‌های زیر ۷۲۰ پیکسل، هاب حساب به **Bottom-Sheet** تبدیل می‌شود و دیالوگ‌های تو در تو هم روی موبایل مرکز و اسکرول‌پذیر می‌مانند.
- سوئیچ‌ها (Toggle) جهت knob صحیح در RTL دارند؛ پشتیبانی از `prefers-reduced-motion` برای کاربران حساس به حرکت.

## ۶. چندزبانه

- بخش هاب به هر ۴ زبان اضافه شد (۴۵ کلید × ۴ زبان: fa/en/ar/zh).

## استقرار (Deploy)

- برای اعمال تغییرات دیتابیس فقط کافی است سرویس را با `npm run start` (یا `start:liara`) بالا بیاورید؛ اسکریپت `scripts/migrate.mjs` به‌صورت خودکار مایگریشن 007 را اجرا می‌کند و نام کاربری کاربران فعلی همان لحظه Backfill می‌شود.
- هیچ Breaking Change در APIهای قبلی نیست؛ فقط endpointهای جدید (`PATCH /api/account/profile`) اضافه شده‌اند.
- **نکته مهم:** اگر `next.config.ts` شما `output: "standalone"` دارد، آن را حذف کنید؛ با جریان استقرار Docker/Liara این پروژه ناسازگار است (تست امنیتی شماره ۲۵ همین را گارد می‌کند).
- Next.js از 16.3.2 به 16.3.4 ارتقا یافت (Patch-Level).

## صحت‌سنجی

- `tsc --noEmit` تمیز، ESLint تمیز، و **۲۵/۲۵ تست امنیتی سبز**.
- تست E2E مرورگر کامل شد: ثبت‌نام ← کارت کاربر ← هاب ← تغییر نام کاربری ← تم روشن ماندگار ← تغییر زبان با جهت RTL/LTR ← سنجه‌های اشتراک ← پنل بیشتر ← خروج ← کارت مهمان ← Bottom-Sheet موبایل با دیالوگ تودرتو.
