# نتیجه بررسی Portal AI 1.7.1

تاریخ: 2026-09-07. آزمون‌ها مربوط به سورس همین بسته‌اند.

| بررسی | نتیجه |
|---|---|
| امنیت، نشست، فایل، استریم، health، Markdown و تنظیمات | 78 تست موفق |
| شروع، migration، سازگاری و گزارش خودکار | 38 تست موفق؛ شامل موتور SQL واقعی |
| راه‌اندازی production از طریق اتصال PostgreSQL TCP | 4 تست/زیرآزمون موفق |
| TypeScript و ESLint | موفق، بدون هشدار کد |
| ساخت production و آماده‌سازی standalone | موفق |
| بررسی تطابق اسکریپت‌ها، قرارداد و SQLهای خروجی مستقل | موفق |
| تطابق قرارداد روی PostgreSQL 16.4 و 17.5 | موفق |

## آزمونی که مستقیماً به لاگ کاربر مربوط است

یک PostgreSQL آزمایشی با اجرای هفت SQL اصلی بدون تغییر از طریق client واقعی `postgres` و اتصال TCP ساخته شد. سپس کاربر آزمایشی، نام مدل ویرایش‌شده مدیر و سه سابقه تاریخی `005_workspace.sql`، `007_security_identity.sql` و `011_legacy_release.sql` ثبت شدند.

1. اسکریپت standalone نسخه 1.7.0 روی همین دیتابیس اجرا شد و همان خطای `007_security_identity.sql` را بازتولید کرد.
2. اسکریپت standalone نسخه 1.7.1 روی همان دیتابیس دو بار اجرا شد، صفحه اصلی پاسخ 200 داد و `/api/health` نسخه درست و `database: ok` را گزارش کرد. تاریخچه، کاربران و تنظیمات مدل قبل و بعد برابر بودند.
3. ستون username حذف و trigger ثبت وقایع غیرفعال شد؛ شروع بعدی با گزارش خودکار همه فایل‌های خارجی و هر دو اختلاف ساختاری متوقف شد. ترمینال یا دستور دستی لازم نبود و اطلاعات کاربر یا رمز در گزارش نبود.

این آزمون از PostgreSQL 18.3 در PGlite 0.5.8 با pgcrypto و PGlite Socket 0.2.11 استفاده می‌کند. PGlite Socket پروتکل اتصال واقعی را فراهم می‌کند، اما مدل چنداتصالی آن با PostgreSQL معمولی یکسان نیست؛ این آزمون، سنجش هم‌زمانی، نقش‌ها یا TLS واقعی لیارا محسوب نمی‌شود. [مستندات رسمی PGlite Socket](https://pglite.dev/docs/pglite-socket).

## رفتار محافظه‌کارانه داده

تاریخچه کامل با نام‌های اضافی، تاریخچه شاخه دیگر با نام‌های اصلی ثبت‌نشده، ساختار موجود بدون جدول تاریخچه و ایندکس هم‌معنا با نام متفاوت آزمایش شدند. مسیر سازگاری هیچ DDL/DML اجرا نکرد و تاریخچه ساختگی نساخت. checksum تغییرکرده، محافظ یکتایی حذف‌شده، trigger خاموش، CHECK اعتبار حذف‌شده و پلن رایگان مفقود، همگی باعث توقف شدند.

قرارداد catalog روی PostgreSQL 16.4 و 17.5 نیز تطبیق داشت. بسته‌های قدیمی WASM افزونه pgcrypto ندارند؛ فقط دستور نصب آن در آزمون مقایسه این دو نسخه کنار گذاشته شد. فایل‌های SQL پروژه تغییر نکردند و این بخش آزمون نصب افزونه روی سرور قدیمی لیارا نیست.

## ایراد health که در همین آزمون پیدا شد

نسخه قبل هنگام اتصال مجدد Redis می‌توانست منتظر بماند و health را با 503 تمام کند، با وجود سالم بودن دیتابیس. اکنون probe اتصال مجدد را منتظر نمی‌ماند و PING آماده نیز deadline دارد. پاسخ `status: degraded` و `redis: unavailable` صریح است. دیتابیس ناسالم همچنان 503 می‌دهد. کنترل‌های نرخ درخواست یا صف تصویر بدون Redis مجاز اعلام نمی‌شوند.

## تکرار آزمون‌ها

```bash
npm ci
npm test
npm run build
npm run validate:artifact
```

برای بخش SQL اختیاری، PGlite 0.5.8 را جدا نصب و `TEST_PGLITE_ROOT` را به پوشه بسته آن اشاره دهید. برای آزمون startup، `TEST_PGLITE_SOCKET_ROOT` نیز مسیر بسته Socket 0.2.11 است و پس از build، `npm run deployment:startup` اجرا می‌شود. `TEST_PREVIOUS_START_SCRIPT` اختیاری است و مسیر اسکریپت standalone نسخه قبلی را برای بازتولید خطای آن می‌گیرد. این‌ها متغیرهای تست محلی هستند و در لیارا لازم نیستند. بدون آن‌ها آزمون‌های اختیاری skip می‌شوند؛ skip به معنی موفقیت آزمون اتصال نیست.

## محدودیت‌ها

- انتشار داخل حساب واقعی لیارا، داده‌های واقعی، AI، S3، پیامک و پرداخت آزموده نشده‌اند.
- هیچ تغییر ظاهری در این اصلاحیه انجام نشده؛ آزمون تصویری جدید ادعا نمی‌شود.
- محدودیت سنجه RSS محیط ساخت با preload موقت همان محیط مدیریت شد؛ آن فایل در ZIP یا تنظیمات استقرار نیست. مراحل واقعی کامپایل، TypeScript و prerender اجرا شدند.
- موفقیت این آزمون‌ها تضمین نبود هر باگ یا سازگاری هر دیتابیس ناشناخته نیست. اختلاف واقعی ساختار یا checksum به‌طور خودکار پنهان نمی‌شود؛ جزئیاتش بدون ترمینال در لاگ می‌آید.

گزارش‌های اجرای این نسخه در `verification/v1.7.1/` هستند. گزارش‌های نسخه‌های قدیمی، سابقه همان انتشارند.
