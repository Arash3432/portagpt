# Portal AI 1.6.0 — گزارش تغییرات

این نسخه روی فایل ZIP پیوست‌شده کاربر ساخته شده است. مسیرهای موجود، منطق حساب، گفتگو، مدل‌ها و هفت migration اصلی حفظ شده‌اند. تغییرات این انتشار روی تجربه کاربری، پایداری، امنیت و ساخت خروجی لیارا متمرکز است. این گزارش ادعای نبود مطلق باگ یا امنیت صددرصدی ندارد.

## درخواست‌های اصلی و نتیجه

| درخواست | پیاده‌سازی |
|---|---|
| منوی کوچک شیشه‌ای کنار دکمه کاربر | کامپوننت مشترک AccountHub در صفحه اصلی، بالای چت و پایین تاریخچه؛ حداکثر ۳۲۰ پیکسل، جای‌گذاری بالا/پایین با توجه به فضای صفحه؛ خارج از بخش‌های بریده‌شده صفحه رندر می‌شود |
| تعامل حرفه‌ای با منو | بستن با کلیک بیرون، Escape و Tab؛ حرکت با کلیدهای جهت؛ بازگردانی فوکوس؛ پنجره‌های جزئیات و تنظیمات با مدیریت فوکوس؛ پشتیبانی RTL و کاهش حرکت |
| تایپ و پاسخ زنده روان | دریافت SSE مستقل از مرز بسته‌های شبکه، UTF-8 و CRLF؛ آشکارشدن متن واقعی با حفظ ایموجی و نویسه‌های ترکیبی؛ به‌روزرسانی هماهنگ با فریم؛ حفظ متن جزئی هنگام قطع اتصال؛ توقف و تلاش مجدد |
| لودینگ حرفه‌ای | وضعیت مجزای متن و تصویر، زمان سپری‌شده واقعی، توقف تایمر در تب پنهان؛ حذف درصد تخمینی تصویر؛ skeleton بارگذاری تصویر و امکان تلاش مجدد |
| دانلود تصویر | دکمه واضح دانلود در کارت تصویر، نمایش اندازه اصلی، حالت دریافت/موفقیت/خطا؛ دانلود احراز‌شده از فایل متعلق به کاربر با نام فایل امن |
| سرعت و پایداری | حذف تأخیر اجباری ۳۲۰ میلی‌ثانیه‌ای تاریخچه؛ عدم معطل‌ماندن ارسال برای به‌روزرسانی مصرف/تاریخچه؛ جلوگیری از ارسال تکراری؛ درخواست‌های قابل توقف و محدودیت زمانی |
| تجربه چت | جست‌وجوی مدل، رفتن به آخرین پیام، حفظ محل مطالعه هنگام پاسخ زنده، دنبال‌کردن رشد تصویر، پیام قطع اینترنت، توقف آپلود، جلوگیری از فایل‌های تکراری، بهبود لمس/فونت موبایل |
| ماندگاری ورود | وضعیت مشترک حساب در حافظه مرورگر، یک درخواست مشترک، حفظ هویت تأییدشده هنگام خطای موقت، همگام‌سازی تب‌ها، بی‌اثرشدن پاسخ‌های قدیمی پس از تغییر حساب |
| نمایش پاسخ | اصلاح لینک‌های Markdown که مقصد و عنوان را جابه‌جا می‌کردند؛ محدودکردن URL به HTTP(S)؛ جدول قابل اسکرول؛ نمایش امن کد و خطای قابل‌مشاهده کپی |
| لیارا | Docker چندمرحله‌ای، Node 22، اجرای غیر root، خروجی standalone کامل، بررسی تنظیمات قبل از شروع، health check دارای timeout و فرصت راه‌اندازی |

## اصلاحات امنیت و بک‌اند

- جلوگیری از تغییر موقعیت IP مورد اعتماد با هدر X-Forwarded-For ناقص؛ همسان‌سازی IPv6 و رد زنجیره ناقص.
- کنترل origin درخواست‌های تغییردهنده، بررسی شکل کوکی و مقایسه معتبر hash؛ پاسخ‌های خطا و لاگ‌های بدون راز.
- اصلاح رقابت تغییر رمز، محدودیت OTP حساب مدیر و بررسی نشست در خواندن APIها.
- خواندن محدودشده JSON و تصویر؛ رد UTF-8 معیوب، کنترل نوع داده و امضای واقعی فایل، محدودیت URL منبع تصویر و لغو جریان اضافی.
- آزادسازی اعتبار رزروشده متن در خطاهای سهمیه/توقف سرویس؛ پوشش timeout بدنه پاسخ تصویر؛ جلوگیری از آزادسازی سهمیه کار اصلی در اجرای تکراری worker.
- فایل دانلود فقط برای مالک نشست؛ Content-Disposition امن، no-store، nosniff و sandbox برای فایل‌ها؛ محتوای فعال به‌صورت attachment ارائه می‌شود.
- حذف نمایش نادرست «تأیید دوباره مدیر» از پنل، زیرا معماری فعلی IP-only است.

## ساخت و migration

- فایل‌های migration موجود بدون تغییر حفظ شده‌اند؛ ترتیب عددی، checksum و کامل‌بودن تاریخچه پیش از اجرا بررسی می‌شود.
- قفل تراکنشی پیش از ایجاد جدول تاریخچه گرفته می‌شود؛ مجموعه migrationهای جدید و تاریخچه در یک تراکنش ثبت می‌شوند.
- تنها خطاهای موقت اتصال/قفل retry می‌شوند. خطای checksum یا تاریخچه با دستکاری جدول پنهان نمی‌شود.
- فایل‌های static، public، migration و اسکریپت‌های شروع همراه با وابستگی مورد نیاز postgres به خروجی مستقل منتقل می‌شوند.
- embedded-postgres، که هیچ استفاده تولیدی نداشت، به وابستگی توسعه منتقل شد.

## نتیجه آزمون‌ها

| بررسی | نتیجه |
|---|---|
| npm ci | موفق |
| تست‌های امنیت، حساب، Markdown، دارایی و استریم | ۷۰ تست موفق |
| تست‌های تنظیمات و migration بدون دیتابیس | ۱۲ تست موفق |
| TypeScript و ESLint کل پروژه | موفق |
| Next.js production build و آماده‌سازی standalone | موفق |
| اعتبارسنجی خروجی و هفت migration | موفق |
| ممیزی وابستگی‌های تولیدی npm | صفر آسیب‌پذیری گزارش‌شده در زمان بررسی |
| تعاملات React در jsdom | ۱۳ بررسی موفق؛ منو، فوکوس، دانلود، خطای کپی و پنجره تو در تو |
| سرور کامپایل‌شده و HTTP | ۱۵ بررسی موفق؛ ۹ صفحه عمومی، فایل JavaScript، عدم ارائه پنل/API مدیر به IP غیرمجاز، نشست مهمان، رد دانلود بدون ورود و رد درخواست chat از origin نامعتبر |

جزئیات روش و محدودیت‌ها در `VALIDATION-1.6.0.md` و لاگ‌های واقعی در `verification/` آمده است.

## شروع استفاده

ابتدا `START-HERE.md` و سپس `LIARA-DEPLOYMENT.md` را بخوانید. سورس را با Dockerfile موجود در لیارا مستقر کنید. node_modules و .next در ZIP سورس نیستند؛ لیارا همان سورس و lockfile را بیلد می‌کند. کم‌بودن حجم ZIP به معنی حذف فایل‌های برنامه نیست.
