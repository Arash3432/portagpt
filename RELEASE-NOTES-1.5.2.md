# Portal AI — Release Notes v1.5.2

**بهبود کوچک و هدفمند: نسخهٔ در حال سرویس‌دهی در `/api/health` نمایش داده می‌شود.**

## چه چیزی اضافه شد

- پاسخ `/api/health` حالا فیلد `version` دارد:
  `{"status":"ok","version":"1.5.2","database":"ok","redis":"ok"}`
- بعد از هر Deploy روی Liara، با یک نگاه به health check می‌فهمید دقیقاً کدام نسخه در حال سرویس‌دهی است — دیگر نیازی به حدس زدن یا چک کردن فایل‌ها نیست. این فیلد از `package.json` خوانده می‌شود و همیشه با بیلد واقعی یکی است.
- در پاسخ 503 (وقتی دیتابیس هنوز آماده نیست) هم نسخه گزارش می‌شود تا در لاگ Liara مشخص باشد کدام بیلد در حال تلاش برای بالا آمدن است.
- یک سناریوی E2E جدید هم به مجموعه تست‌ها اضافه شد که وجود و درستی همین فیلد را روی سرور production تأیید می‌کند (۱۳/۱۳ ✓).

## ارتقا

- هیچ تغییر دیگری نسبت به 1.5.1 وجود ندارد؛ ارتقا = جایگزینی سورس و Deploy مجدد.
