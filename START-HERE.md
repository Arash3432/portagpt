# شروع استقرار Portal AI 1.7.1 روی لیارا

اگر برنامه از قبل در لیارا دارید، ابتدا [راهنمای ارتقا](UPGRADE-1.7.1.md) را بخوانید. برای رفع خطاهای تاریخچه دیتابیس به متغیر جدید نیاز نیست؛ Secretها و اتصال دیتابیس قبلی را حفظ کنید. مراحل ساخت سرویس و تولید Secret در پایین فقط برای نصب اولیه است.

1. در لیارا یک برنامه **Docker** با پورت **3000** بسازید. PostgreSQL و Redis برنامه را در شبکه خصوصی مناسب ایجاد کنید. برای آپلود و ساخت تصویر، Object Storage با bucket خصوصی هم لازم است.
2. مقدار اتصال سرویس‌ها، `PUBLIC_APP_URL` با دامنه نهایی HTTPS و متغیرهای `.env.example` را در بخش Environment برنامه وارد کنید. فایل نمونه را بدون جایگزین‌کردن مقدارهای نمونه استفاده نکنید.
3. داخل پوشه پروژه `npm run secrets:generate` را اجرا کنید، یا `GENERATE-SECRETS.html` را آفلاین باز کنید. سه خروجی را فقط در Environment لیارا ثبت کنید. این مقدارها را در استقرارهای بعدی ثابت نگه دارید؛ عوض‌کردن کلید رمزگذاری دسترسی به کلید AI ذخیره‌شده را از بین می‌برد.
4. `ADMIN_IP_ALLOWLIST` را روی IP عمومی ثابت و اختصاصی مدیر بگذارید. پنل فعلی **IP-only** است: IP مجاز بدون رمز وارد می‌شود؛ هر شخص دیگری پشت همان IP هم دسترسی مدیر دارد. VPN عمومی یا خروجی مشترک برای این دسترسی مناسب نیست. مقدار خالی، مدیریت را برای همه می‌بندد.
5. آدرس واقعی Liara AI حساب خودتان را در `AI_BASE_URL` و فقط hostname آن را در `AI_PROVIDER_HOST_ALLOWLIST` وارد کنید. کلید را بعد از استقرار در بخش ارائه‌دهنده پنل ذخیره کنید، یا برای اولین بار از متغیر محرمانه `AI_API_KEY` استفاده کنید.
6. پوشه پروژه شامل `Dockerfile` و `liara.json` را ZIP و در برنامه Docker لیارا بارگذاری کنید. لازم نیست `node_modules` یا خروجی `.next` محلی را آپلود کنید؛ Docker در لیارا همان سورس را می‌سازد.
7. لاگ شروع باید به ترتیب `portal.preflight` با `ok: true` و `migration.ready` را نشان دهد. بعد `/api/health` را باز کنید. سلامت داده، Redis، ورود، AI و فضای تصویر را طبق راهنمای استقرار بررسی کنید.
8. از IP مجاز `/admin` را باز کنید و اتصال ارائه‌دهنده را تست کنید. سپس یک گفت‌وگو، یک تصویر و دانلود همان تصویر را روی موبایل و دسکتاپ امتحان کنید.

`ADMIN_PHONE_E164` و `ADMIN_PASSWORD` برای حالت فعلی پنل کاربرد ندارند. `TRUSTED_PROXY_HOPS` تعداد proxyهای مورد اعتماد از سمت راست زنجیره `X-Forwarded-For` است؛ مقدار یک فقط وقتی درست است که یک ingress مورد اعتماد در جلوی برنامه باشد. با افزودن CDN یا proxy، این مقدار باید با مسیر واقعی تطبیق داده شود.

راهنمای کامل، خطاهای شروع و محدودیت‌های بررسی در [LIARA-DEPLOYMENT.md](LIARA-DEPLOYMENT.md) آمده است. راهنمای عملیات مدیر در [ADMIN-GUIDE.md](ADMIN-GUIDE.md) است.
