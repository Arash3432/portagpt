# چک‌لیست انتشار Portal AI 1.7.1

- [ ] PostgreSQL بکاپ دارد و نسخه schema با این سورس سازگار است.
- [ ] `npm ci`، `npm test`، `npm run build` و `npm run validate:artifact` موفق‌اند.
- [ ] تست Docker یا انتشار آزمایشی در لیارا با Node 22 و پورت 3000 انجام شده است.
- [ ] `npm run preflight` با Environment مقصد موفق است و مقدارهای نمونه جایگزین شده‌اند.
- [ ] Secretهای نشست متفاوت‌اند و کلید رمزگذاری قبلی حفظ شده است.
- [ ] اتصال PostgreSQL و Redis واقعی و فضای S3 خصوصی کامل تنظیم شده‌اند.
- [ ] پنل IP-only فقط از IP عمومی ثابت و اختصاصی مدیر در دسترس است؛ خروجی مشترک استفاده نمی‌شود.
- [ ] توپولوژی proxy و تعداد `TRUSTED_PROXY_HOPS` بررسی شده‌اند؛ هدر X-Forwarded-For ساختگی از بیرون دسترسی مدیر نمی‌دهد.
- [ ] health نسخه صحیح، دیتابیس سالم و Redis سالم را گزارش می‌کند.
- [ ] ورود، حفظ نشست، خروج، منوی حساب، گفتگو، توقف استریم و خطای شبکه بررسی شده‌اند.
- [ ] تولید، نمایش و دانلود تصویر واقعی و رد دسترسی حساب دیگر بررسی شده‌اند.
- [ ] کلید/مدل‌های AI با حساب واقعی، سقف هزینه و قیمت ارائه‌دهنده هماهنگ‌اند.
- [ ] پرداخت تا تأیید آزمون درگاه خاموش است.

روش اجرای هر مرحله و رفع خطا در [LIARA-DEPLOYMENT.md](LIARA-DEPLOYMENT.md) آمده است. موارد انجام‌نشده را به عنوان «تأیید نشده» ثبت کنید؛ build موفق به معنی استقرار واقعی موفق نیست.
