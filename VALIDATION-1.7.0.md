# گزارش بررسی نسخه 1.7.0

تاریخ: 2026-09-07. این گزارش مربوط به سورس همین ZIP است و نتیجه استقرار در حساب واقعی لیارا نیست.

| بررسی | نتیجه |
|---|---|
| تست‌های امنیت، نشست، دانلود، Markdown، استریم، تاریخچه و تنظیم حالت نمایش | 76 تست موفق |
| تست‌های شروع، migration و گزارش سازگاری | 31 تست موفق؛ شامل اجرای موتور SQL واقعی، بدون skip در اجرای گزارش‌شده |
| TypeScript و ESLint | موفق |
| ساخت بهینه Next.js و آماده‌سازی standalone | موفق |
| اعتبارسنجی بسته مستقل، اسکریپت‌ها، قرارداد schema، فایل‌های عمومی و نسخه | موفق |
| تعامل واقعی React با API و هندسه شبیه‌سازی‌شده در jsdom | 28 بررسی موفق |
| درخواست HTTP به سرور production ساخته‌شده | 17 بررسی موفق |
| `npm audit --omit=dev --audit-level=high` | صفر آسیب‌پذیری گزارش‌شده در dependencyهای production در زمان بررسی |

## آزمون دیتابیس گزارش‌شده

هر هفت migration اصلی با بایت‌های قبلی روی PostgreSQL 18.3 در PGlite 0.5.8 و همراه افزونه pgcrypto اجرا شدند. داده آزمایشی کاربر و تغییر مدیر روی مدل ثبت شد؛ سپس سابقه `005_workspace.sql` به سناریوی آزمایش اضافه شد. دو شروع متوالی از همان کد تراکنش برنامه، هیچ DDL/DML اجرا نکردند و داده‌ها و تاریخچه ثابت ماندند.

آزمون‌های منفی شامل migration اصلی ثبت‌نشده، checksum تغییرکرده، سابقه ناشناخته اضافی، حذف محافظ یکتایی پیام، غیرفعال‌کردن trigger حسابرسی و حذف CHECK اعتبار بودند؛ همه به توقف منجر شدند. گزارش `db:status` از همان سیاست سازگاری شروع استفاده می‌کند.

ساختار catalog PostgreSQL 16.4 و 17.5 نیز با قرارداد تطبیق داشت. در آزمون این دو بسته قدیمی WASM، فقط نصب pgcrypto به دلیل نبود افزونه در بسته آزمایش کنار گذاشته شد؛ این نتیجه، تأیید نصب افزونه در PostgreSQL قدیمی لیارا نیست. توضیح کامل در [MIGRATION-RECOVERY.md](MIGRATION-RECOVERY.md) است.

برای تکرار تست اختیاری موتور SQL می‌توان PGlite 0.5.8 را در پوشه آزمایشی جدا نصب کرد و `TEST_PGLITE_ROOT` را به پوشه بسته آن داد. این متغیر فقط برای تست است؛ به لیارا اضافه نکنید. بدون آن، تست موتور SQL در `npm test` skip می‌شود. تست جداگانه `deployment:integration` نیز نیازمند `TEST_DATABASE_URL` آزمایشی است؛ اتصال شبکه و هم‌زمانی چند اتصال واقعی PostgreSQL در این بررسی اجرا نشده‌اند.

## تعامل و HTTP

آزمون‌های jsdom روی خود کامپوننت‌ها اجرا شدند: فوکوس و صفحه‌کلید منوی حساب، پنجره‌های تو‌در‌تو، دانلود/خطا/نشست منقضی و لغو درخواست تصویر، انتقال و پاک‌کردن fragment متن اولیه بدون ارسال خودکار، حفظ متن پس از ورود، محدودیت تصویر، زبان ذخیره‌شده، زبانه‌های صفحه شروع، ذخیره تنظیمات، تأیید حذف و خطای مستقل مصرف حساب.

آزمون HTTP، سرور مستقل ساخته‌شده را اجرا کرد و نه مسیر عمومی، فایل‌های CSS/JavaScript و اثر نمونه استودیو را دریافت کرد. هدرهای امنیتی، رد API مدیریت از IP غیرمجاز، رد دانلود بدون نشست و رد درخواست تغییر از origin دیگر بررسی شدند. صفحه مدیریتِ مسدود در حالت streaming می‌تواند HTTP 200 با محتوای 404 داشته باشد؛ تست، نمایش پنل را نیز رد می‌کند. این آزمون به سرویس AI، S3، Redis و دیتابیس واقعی شما متصل نشد.

## محدودیت‌های روشن

- مرورگر ابری، نشانی‌های پیش‌نمایش محلی را طبق سیاست دسترسی خود مسدود کرد. آزمون تصویری رندر موبایل/دسکتاپ در مرورگر انجام نشد؛ بررسی واکنش‌گرایی در سورس و تعامل‌ها در jsdom انجام شد. هندسه jsdom، اثبات صحت پیکسلی نیست.
- در محیط ساخت، فراخوانی سنجه حافظه Node خطای محدودیت sandbox داشت. یک preload موقت فقط برای گزارش RSS در همان محیط استفاده شد؛ در ZIP و تنظیمات لیارا قرار ندارد. کامپایل، بررسی نوع، prerender و آماده‌سازی standalone واقعی اجرا شدند.
- image نهایی Docker در لیارا از سورس ساخته می‌شود. Docker daemon و سرویس‌های واقعی حساب شما از این محیط آزموده نشده‌اند.
- فایل اصلی تاریخی `005_workspace.sql` در اختیار نبود؛ سازگاری مشروط ساختار بررسی شده است، نه محتوای آن فایل. در صورت نبود شروط، برنامه به جای تغییر حدسی دیتابیس متوقف می‌شود.
- audit dependency و تست‌های هدفمند، تضمین امنیت کامل یا نبود همه باگ‌ها نیستند. کنترل مدیریت این پروژه همچنان IP-only است؛ راهنمای موجود آن را توضیح می‌دهد.

گزارش‌های خام این نسخه در `verification/v1.7.0/` هستند. گزارش‌های شماره‌دار قبلی صرفاً تاریخچه همان نسخه‌اند.
