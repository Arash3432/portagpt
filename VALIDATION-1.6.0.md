# اعتبارسنجی Portal AI 1.6.0

تاریخ بررسی: ۷ سپتامبر ۲۰۲۶. محیط کامپایل محلی: Linux x64 و Node 24.19.0؛ Dockerfile مقصد از Node 22 Alpine استفاده می‌کند. اجرای خود Docker/Alpine در این محیط آزموده نشده است.

## شواهد همراه

- `verification/test-final.log`: ۷۰ تست TypeScript/TSX و ۱۲ تست استقرار، سپس TypeScript و lint.
- `verification/build-final.log`: کامپایل تولیدی، بررسی TypeScript، تولید مسیرها و آماده‌سازی standalone.
- `verification/npm-audit-final.json`: نتیجه ممیزی وابستگی‌های تولیدی؛ فقط آسیب‌پذیری‌های شناخته‌شده فهرست npm را پوشش می‌دهد.
- `verification/smoke-result.json`: ۱۵ بررسی HTTP روی اجرای مستقیم سرور کامپایل‌شده با تنظیمات آزمایشی؛ بدون migration و خدمات واقعی.
- `verification/artifact-validation.txt`: بررسی ساختار خروجی، نسخه، اسکریپت‌ها و بایت‌های migrationها.

## محدوده آزمون رابط

۱۳ تعامل با React و jsdom اجرا شد: جای‌گذاری منو با اندازه‌های شبیه‌سازی‌شده، کیبورد، پنجره تو در تو و بازگشت فوکوس، بستن بیرونی، دانلود تصویر و جلوگیری از کلیک تکراری، رد HTML و URL بیرونی، خطای نشست، لغو دانلود و آزادسازی object URL، خطای clipboard. این روش مرورگر واقعی نیست و کیفیت بصری، چیدمان واقعی موبایل، Safari یا دانلود واقعی در دستگاه کاربر را ثابت نمی‌کند. مرورگر متصل اجازه بازکردن localhost این محیط را نداد؛ بررسی بصری زنده انجام نشد.

## شرایط خاص محیط بیلد

فراخوانی آمار RSS در Node این محیط با `ENOENT / uv_resident_set_memory` خطا داشت و Next پیش از تکمیل build متوقف می‌شد. برای اجرای تست از preload جداگانه‌ای استفاده شد که فقط در همین خطا آمار heap را از V8 می‌گیرد و مقدار RSS را صفر قرار می‌دهد. هیچ مرحله کامپایل، TypeScript، lint، تولید صفحه یا آماده‌سازی خروجی حذف نشده است. این preload بخشی از سورس تحویلی، Dockerfile یا فرمان production نیست؛ اجرای عادی در لیارا باید دوباره تأیید شود.

## بررسی‌های انجام‌نشده و شروط عرضه

۱. استقرار واقعی در حساب لیارا انجام نشده است. PostgreSQL، Redis، Object Storage، ارائه‌دهنده AI، SMS و درگاه واقعی در اختیار آزمون نبودند.
۲. سه تست integration دیتابیس در پروژه وجود دارد؛ برای اجرای آن‌ها باید `TEST_DATABASE_URL` به دیتابیس آزمایشی دورریختنی اشاره کند. در این محیط اجرا نشدند. ساخت دیتابیس محلی به‌دلیل محدودیت اجرای غیر root ممکن نشد.
۳. ورود واقعی، رزرو/تسویه سهمیه هم‌زمان، آپلود به S3، تصویر واقعی و دانلود آن باید روی staging بررسی شوند. پاسخ آزمایشی دانلود در DOM، تأیید عملکرد ارائه‌دهنده واقعی نیست.
۴. تست بار گسترده، تست نفوذ کامل و تضمین نبود باگ انجام نشده است.
۵. مدیریت همچنان IP-only است: همه کاربران پشت IP مجاز دسترسی دارند. ingress مورد اعتماد، تعداد درست TRUSTED_PROXY_HOPS و جلوگیری از دسترسی مستقیم به برنامه الزامی است. معماری فعلی جایگزین رمز و MFA نیست.
۶. CSP برای اجرای فعلی Next هنوز اجازه inline script می‌دهد. هدرهای جدید و سایر اصلاحات به معنی CSP مبتنی بر nonce نیستند.
۷. صفحه admin در پاسخ streaming ممکن است HTTP 200 همراه با نمای Not Found برگرداند؛ در تست، HTML پنل ارائه نشد و API مدیریت برای IP غیرمجاز 404 داد.

## تکرار بررسی‌ها

```bash
npm ci
npm test
npm run build
npm run validate:artifact
npm run audit:production
```

در محیط staging مطابق `LIARA-DEPLOYMENT.md`، ابتدا preflight و سپس آزمون سرویس‌های واقعی و مرورگر موبایل/دسکتاپ را اجرا کنید. `npm start` تنظیمات production را بررسی و سپس migration را اجرا می‌کند؛ پیش از اجرای آن روی دیتابیس اصلی، بکاپ بگیرید.
