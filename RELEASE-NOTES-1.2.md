# Portal AI 1.2 — بازطراحی جدی محصول

این نسخه یک اصلاح ظاهری کوچک نیست؛ ساختار تجربه کاربر، صفحات محصول و لایه ورود مدیر بازطراحی شده است.

## تغییرات اصلی

- صفحه خانه با روایت انسانی‌تر، فضای تنفس بیشتر و نمایش واقعی کاربردهای گفتگو، کدنویسی و تصویر؛
- صفحه مستقل مدل‌ها با جستجو، فیلتر، توضیح کاربرد و انتخاب مستقیم مدل؛
- صفحه کامل اشتراک‌ها با معرفی، مقایسه، سهمیه‌ها، پرسش‌های رایج و قیمت زنده از پنل مدیریت؛
- صفحه حساب با نمایش مصرف و مدیریت نشست‌های فعال؛
- صفحه تنظیمات برای تم، تراکم رابط، کاهش حرکت، مدل پیش‌فرض و رفتار کلید Enter؛
- استودیوی تصویر مستقل و لینک‌های روشن میان بخش‌های محصول؛
- صفحه‌های حریم خصوصی و قوانین؛
- فونت فارسی self-hosted و رابط responsive برای موبایل، تبلت و دسکتاپ؛
- جایگزینی پیام‌های ساده مرورگر با Toast، تأیید و دیالوگ‌های اختصاصی؛
- حذف توضیحات فنی نامرتبط از مسیر عادی کاربران؛
- ورود و ثبت‌نام مبتنی بر رمز، با امکان استفاده اختیاری از پیامک؛
- تغییر رمز و خروج از دستگاه‌های دیگر از داخل حساب؛
- نشست ۱۲ ساعته مدیر، یک دستگاه هم‌زمان، اتصال نشست به شبکه و مرورگر، محدودیت تلاش جداگانه و ثبت رخدادهای مشکوک؛
- ورود روان مدیر و تأیید دوباره فقط برای تغییرات حساس پس از پایان بازه مجاز؛
- نسخه‌گذاری پروژه روی `1.2.0`.

## استقرار

فایل ZIP این نسخه را به‌عنوان استقرار جدید Docker در لیارا بارگذاری کنید. متغیرهای Environment/Secrets فعلی حذف یا داخل ZIP قرار داده نشده‌اند و باید همان‌طور در پنل لیارا باقی بمانند. پس از استقرار، ابتدا `/api/health`، سپس `/plans`، `/models`، `/app` و `/admin` را بررسی کنید.
